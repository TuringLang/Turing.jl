var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/Optimisation/#API:-Turing.Optimisation","page":"Optimisation","text":"","title":"API: Turing.Optimisation"},{"category":"type","location":"api/Optimisation/#DynamicPPL.InitFromParams","page":"Optimisation","text":"InitFromParams(\n    m::ModeResult,\n    fallback::Union{AbstractInitStrategy,Nothing}=InitFromPrior()\n)\n\nInitialize a model from the parameters stored in a ModeResult. The fallback is used if some parameters are missing from the ModeResult.\n\n\n\n\n\n","title":"DynamicPPL.InitFromParams"},{"category":"type","location":"api/Optimisation/#Turing.Optimisation.InitWithConstraintCheck","page":"Optimisation","text":"InitWithConstraintCheck(lb, ub, actual_strategy) <: AbstractInitStrategy\n\nInitialise parameters with actual_strategy, but check that the initialised parameters satisfy any bounds in lb and ub.\n\n\n\n\n\n","title":"Turing.Optimisation.InitWithConstraintCheck"},{"category":"type","location":"api/Optimisation/#Turing.Optimisation.MAP","page":"Optimisation","text":"MAP <: ModeEstimator\n\nConcrete type for maximum a posteriori estimation.\n\n\n\n\n\n","title":"Turing.Optimisation.MAP"},{"category":"type","location":"api/Optimisation/#Turing.Optimisation.MLE","page":"Optimisation","text":"MLE <: ModeEstimator\n\nConcrete type for maximum likelihood estimation.\n\n\n\n\n\n","title":"Turing.Optimisation.MLE"},{"category":"type","location":"api/Optimisation/#Turing.Optimisation.ModeEstimator","page":"Optimisation","text":"ModeEstimator\n\nAn abstract type to mark whether mode estimation is to be done with maximum a posteriori (MAP) or maximum likelihood estimation (MLE).\n\n\n\n\n\n","title":"Turing.Optimisation.ModeEstimator"},{"category":"type","location":"api/Optimisation/#Turing.Optimisation.ModeResult","page":"Optimisation","text":"ModeResult{\n    E<:ModeEstimator,\n    P<:DynamicPPL.VarNamedTuple,\n    LP<:Real,\n    L<:DynamicPPL.LogDensityFunction,\n    O<:Any,\n}\n\nA wrapper struct to store various results from a MAP or MLE estimation.\n\nFields\n\nestimator::Turing.Optimisation.ModeEstimator: The type of mode estimation (MAP or MLE).\nparams::VarNamedTuple: Dictionary of parameter values. These values are always provided in unlinked space,     even if the optimisation was run in linked space.\nlp::Real: The final log likelihood or log joint, depending on whether MAP or MLE was run.     Note that this is the actual log probability of the parameters, i.e., not negated;     we do need a negated log probability to run the optimisation itself (since it is a     maximisation), but this is handled in a way that is entirely transparent to the user.\nlinked::Bool: Whether the optimisation was done in a transformed space.\nldf::LogDensityFunction: The LogDensityFunction used to calculate the output. Note that this LogDensityFunction     calculates the actual (non-negated) log density. It should hold that m.lp ==     LogDensityProblems.logdensity(m.ldf, m.optim_result.u) for a ModeResult m.\nThe objective function used for minimisation is equivalent to `p ->\n-LogDensityProblems.logdensity(m.ldf, p)`). Note, however, that `p` has to be provided\nas a vector in linked or unlinked space depending on the value of `m.linked`.\n\nIf `m.linked` is true, to evaluate the log-density using unlinked parameters, you\ncan use `logjoint(m.ldf.model, params)` where `params` is a NamedTuple or Dictionary\nof unlinked parameters.\noptim_result::Any: The stored optimiser results.\n\n\n\n\n\n","title":"Turing.Optimisation.ModeResult"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.ModeResult-Tuple{LogDensityFunction, SciMLBase.OptimizationSolution, Bool, Turing.Optimisation.ModeEstimator}","page":"Optimisation","text":"ModeResult(\n    log_density::DynamicPPL.LogDensityFunction,\n    solution::SciMLBase.OptimizationSolution,\n    linked::Bool,\n    estimator::ModeEstimator,\n)\n\nCreate a ModeResult for a given log_density objective and a solution given by solve. The linked argument indicates whether the optimization was done in a transformed space.\n\nOptimization.solve returns its own result type. This function converts that into the richer format of ModeResult. It also takes care of transforming them back to the original parameter space in case the optimization was done in a transformed space.\n\n\n\n\n\n","title":"Turing.Optimisation.ModeResult"},{"category":"method","location":"api/Optimisation/#StatsAPI.coeftable-Tuple{Turing.Optimisation.ModeResult}","page":"Optimisation","text":"StatsBase.coeftable(m::ModeResult; level::Real=0.95, numerrors_warnonly::Bool=true)\n\nReturn a table with coefficients and related statistics of the model. level determines the level for confidence intervals (by default, 95%).\n\nIn case the numerrors_warnonly argument is true (the default) numerical errors encountered during the computation of the standard errors will be caught and reported in an extra \"Error notes\" column.\n\n\n\n\n\n","title":"StatsAPI.coeftable"},{"category":"method","location":"api/Optimisation/#StatsAPI.informationmatrix-Tuple{Turing.Optimisation.ModeResult}","page":"Optimisation","text":"StatsBase.informationmatrix(\n    m::ModeResult;\n    adtype::ADTypes.AbstractADType=ADTypes.AutoForwardDiff()\n)\n\nCalculate the Fisher information matrix for the mode result m. This is the negative Hessian of the log-probability at the mode.\n\nThe Hessian is calculated using automatic differentiation with the specified adtype. By default this is ADTypes.AutoForwardDiff(), which computes the Hessian via nested forward-mode differentiation. Any adtype that supports second-order differentiation may be supplied instead.\n\n\n\n\n\n","title":"StatsAPI.informationmatrix"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.check_bound_covers-Tuple{VarNamedTuple, Any, Any, Any}","page":"Optimisation","text":"check_bound_covers(constraints::VarNamedTuple, name, vn, range)\n\nThrow unless constraints bounds every element of vn.\n\nA bound is honoured only if it covers a whole variable as the model writes it, and the count of the caller's keys falling under vn is what says whether it does. Comparing the length of the assembled bound vector instead caught only some of the ways it can fall short: the accumulator sizes that vector from the highest index the caller mentioned, so Dict(@varname(x[1]) => 0.5) against x ~ MvNormal(zeros(3), I) produced a one-element vector and was caught, while x[2] alone, or x[1] together with x[3], produced a three-element one that passed and left the unmentioned slots unbounded – the mode came back at 0.25 for every element under a lower bound of 0.5.\n\n\n\n\n\n","title":"Turing.Optimisation.check_bound_covers"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.check_constraints_reached-Tuple{VarNamedTuple, Any, Any, Any}","page":"Optimisation","text":"check_constraints_reached(constraints::VarNamedTuple, name, applied, model_vns)\n\nComplain about any bound in constraints that was not applied.\n\napplied is the list of variables a bound was actually written for, taken from the assembly rather than predicted: a bound reaches the optimiser only if it arrives there, and asking get_constraints whether the collection could answer for a variable is not the same question. A bound on non-leading elements of a variable the model writes whole – x[2], or x[1] together with x[3], against x ~ MvNormal(zeros(3), I) – never arrives, and was reported as used while the mode came back unconstrained.\n\nTwo granularities have to be reconciled, and getting that wrong made an earlier version of this check refuse bounds that work. keys(constraints) enumerates leaves, so a bound written Dict(@varname(x[1:2]) => [0.9, 0.9]) enumerates as x[1], x[2], while applied and model_vns hold whole tilde VarNames, which may be compound. A leaf is matched against them by subsumption in either direction, which compares the two at the same granularity.\n\nAn unapplied bound is fatal when the model does reach a variable covering it, since that is a malformed bound: bounding part of a variable the model writes as one value cannot be honoured, and asking for it is a mistake worth stopping for. When no reached variable covers it the bound is merely moot – the key may name a variable that is conditioned, fixed, or in a branch not taken, which is indistinguishable here from one naming nothing at all – so that warns, and the mode is still correctly constrained on the variables that do exist.\n\n\n\n\n\n","title":"Turing.Optimisation.check_constraints_reached"},{"category":"function","location":"api/Optimisation/#Turing.Optimisation.estimate_mode","page":"Optimisation","text":"estimate_mode(\n    [rng::Random.AbstractRNG,]\n    model::DynamicPPL.Model,\n    estimator::ModeEstimator,\n    solver=OptimizationOptimJL.LBFGS();\n    link::Bool=true,\n    initial_params=DynamicPPL.InitFromPrior(),\n    lb::Union{NamedTuple,AbstractDict{<:VarName,<:Any}}=(;),\n    ub::Union{NamedTuple,AbstractDict{<:VarName,<:Any}}=(;),\n    adtype::AbstractADType=AutoForwardDiff(),\n    check_model::Bool=true,\n    check_constraints_at_runtime::Bool=true,\n    kwargs...,\n)\n\nFind the mode of the probability distribution of a model.\n\nUnder the hood this function constructs a LogDensityFunction and calls Optimization.solve on it.\n\nNote that the optimisation interface that Turing exposes is a more high-level interface which is tailored towards probabilistic modelling, so not every option available in Optimization.jl is supported here. In particular, Turing's optimisation interface allows you to:\n\nProvide initial parameters, lower bounds, and upper bounds as mappings of VarNames to values in original (unlinked space).\nChoose whether to run the optimisation in linked or unlinked space (by default linked). Linked space means that parameters are transformed to unconstrained Euclidean space, meaning that you can avoid hard edges in the optimisation landscape (i.e., logpdf suddenly dropping to -Inf outside the support of a variable). It also avoids cases where parameters may not be independent, e.g., x ~ Dirichlet(...) where the components of x must sum to 1. Optimisation in linked space is enabled by default.\n\nTuring is responsible for 'translating' these user-friendly specifications into vectorised forms (of initial parameters, lower bounds, and upper bounds) that Optimization.jl can work with.\n\nHowever, there are cases where this translation can fail or otherwise be ill-defined (specifically when considering constraints). For example, recall that constraints are supplied in unlinked space, but the optimisation is run by default in linked space. Sometimes it is possible to translate constraints from unlinked space to linked space: for example, for x ~ Beta(2, 2), lower bounds in unlinked space can be translated to lower bounds in linked space via the logit transform (specifically, by calling Bijectors.VectorBijectors.to_linked_vec(Beta(2, 2)).\n\nHowever, if a user supplies a constraint on a Dirichlet variable, there is no well-defined mapping of unlinked constraints to linked space. In such cases, Turing will throw an error (although you can still run in unlinked space). Generic, non-box constraints are also not possible to correctly support, so Turing's optimisation interface refuses to support them.\n\nSee https://github.com/TuringLang/Turing.jl/issues/2634 for more discussion on the interface and what it supports.\n\nIf you need these capabilities, we suggest that you create your own LogDensityFunction and call Optimization.jl directly on it.\n\nArguments\n\nrng::Random.AbstractRNG: an optional random number generator. This is used only for parameter initialisation; it does not affect the actual optimisation process.\nmodel::DynamicPPL.Model: The model for which to estimate the mode.\nestimator::ModeEstimator: Can be either MLE() for maximum likelihood estimation or MAP() for maximum a posteriori estimation.\nsolver=OptimizationOptimJL.LBFGS(): The optimization algorithm to use. The default solver is L-BFGS, which is a good general-purpose solver that supports box constraints. You can also use any solver supported by Optimization.jl.\n\nKeyword arguments\n\nlink::Bool=true: if true, the model parameters are transformed to an unconstrained space for the optimisation. This is generally recommended as it avoids hard edges (i.e., returning a probability of Inf outside the support of the parameters), which can lead to NaN's or incorrect results. Note that the returned parameter values are always in the original (unlinked) space, regardless of whether link is true or false.\ninitial_params::DynamicPPL.AbstractInitStrategy=DynamicPPL.InitFromPrior(): an initialisation strategy for the parameters. By default, parameters are initialised by generating from the prior. The initialisation strategy will always be augmented by any constraints provided via lb and ub, in that the initial parameters will be guaranteed to lie within the provided bounds.\nlb::Union{NamedTuple,AbstractDict{<:VarName,<:Any}}=(;): a mapping from variable names to lower bounds for the optimisation. The bounds should be provided in the original (unlinked) space. Not all constraints are supported by Turing's optimisation interface. See details above.\nub::Union{NamedTuple,AbstractDict{<:VarName,<:Any}}=(;): a mapping from variable names to upper bounds for the optimisation. The bounds should be provided in the original (unlinked) space. Not all constraints are supported by Turing's optimisation interface. See details above.\nadtype::AbstractADType=AutoForwardDiff(): The automatic differentiation backend to use.\ncheck_model::Bool=true: if true, the model is checked for potential errors before optimisation begins.\ncheck_constraints_at_runtime::Bool=true: if true, the constraints provided via lb  and ub are checked at each evaluation of the log probability during optimisation (even  though Optimization.jl already has access to these constraints). This can be useful in a  very specific situation: consider a model where a variable has a dynamic support, e.g.  y ~ truncated(Normal(); lower=x), where x is another variable in the model. In this  case, if the model is run in linked space, then the box constraints that Optimization.jl  sees may not always be correct, and y may go out of its intended bounds due to changes  in x. Enabling this option will ensure that such violations are caught and an error  thrown. This is very cheap to do, but if you absolutely need to squeeze out every last  bit of performance and you know you will not be hitting the edge case above, you can  disable this check.\n\nAny extra keyword arguments are passed to Optimization.solve.\n\n\n\n\n\n","title":"Turing.Optimisation.estimate_mode"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.make_optim_bounds_and_init-Tuple{Random.AbstractRNG, LogDensityFunction, DynamicPPL.AbstractInitStrategy, VarNamedTuple, VarNamedTuple}","page":"Optimisation","text":"make_optim_bounds_and_init(\n    rng::Random.AbstractRNG,\n    ldf::LogDensityFunction,\n    initial_params::AbstractInitStrategy,\n    lb::VarNamedTuple,\n    ub::VarNamedTuple,\n)\n\nGenerate a tuple of (lb_vec, ub_vec, init_vec) which are suitable for directly passing to Optimization.jl. All three vectors returned will be in the unlinked or linked space depending on ldf.transform_strategy, which in turn is defined by the value of link passed to mode_estimate.\n\nThe lb and ub arguments, as well as any initial_params provided as InitFromParams, are expected to be in the unlinked space.\n\n\n\n\n\n","title":"Turing.Optimisation.make_optim_bounds_and_init"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.maximum_a_posteriori-Tuple{Random.AbstractRNG, DynamicPPL.Model, Vararg{Any}}","page":"Optimisation","text":"maximum_a_posteriori(\n    [rng::Random.AbstractRNG,]\n    model::DynamicPPL.Model,\n    [solver];\n    kwargs...\n)\n\nFind the maximum a posteriori estimate of a model.\n\nThis is a convenience function that calls estimate_mode with MAP() as the estimator. Please see the documentation of Turing.Optimisation.estimate_mode for full details.\n\n\n\n\n\n","title":"Turing.Optimisation.maximum_a_posteriori"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.maximum_likelihood-Tuple{Random.AbstractRNG, DynamicPPL.Model, Vararg{Any}}","page":"Optimisation","text":"maximum_likelihood(\n    [rng::Random.AbstractRNG,]\n    model::DynamicPPL.Model,\n    [solver];\n    kwargs...\n)\n\nFind the maximum likelihood estimate of a model.\n\nThis is a convenience function that calls estimate_mode with MLE() as the estimator. Please see the documentation of Turing.Optimisation.estimate_mode for full details.\n\n\n\n\n\n","title":"Turing.Optimisation.maximum_likelihood"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.satisfies_constraints-Tuple{Union{Nothing, Real}, Union{Nothing, Real}, Real, UnivariateDistribution}","page":"Optimisation","text":"satisfies_constraints(lb, ub, proposed_val, dist)\n\nCheck whether proposed_val satisfies the constraints defined by lb and ub.\n\nThe methods that this function provides therefore dictate what values users can specify for different types of distributions. For example, for UnivariateDistribution, the constraints must be supplied as Real numbers. If other kinds of constraints are given, it will hit the fallback method and an error will be thrown.\n\nThis method intentionally does not handle NaN values as that is left to the optimiser to deal with.\n\n\n\n\n\n","title":"Turing.Optimisation.satisfies_constraints"},{"category":"method","location":"api/Optimisation/#Turing.Optimisation.vector_names_and_params-Tuple{Turing.Optimisation.ModeResult}","page":"Optimisation","text":"vector_names_and_params(m::ModeResult)\n\nGenerates a vectorised form of the optimised parameters stored in the ModeResult, along with the corresponding variable names. These parameters correspond to unlinked space.\n\nThis function returns two vectors: the first contains the variable names, and the second contains the corresponding values.\n\n\n\n\n\n","title":"Turing.Optimisation.vector_names_and_params"},{"category":"section","location":"api/Inference/#API:-Turing.Inference","page":"Inference","text":"","title":"API: Turing.Inference"},{"category":"type","location":"api/Inference/#Turing.Inference.CSMC","page":"Inference","text":"Conditional SMC, an alias for PG.\n\n\n\n\n\n","title":"Turing.Inference.CSMC"},{"category":"type","location":"api/Inference/#Turing.Inference.ESS","page":"Inference","text":"ESS\n\nElliptical slice sampling algorithm.\n\nExamples\n\njulia> @model function gdemo(x)\n           m ~ Normal()\n           x ~ Normal(m, 0.5)\n       end\ngdemo (generic function with 2 methods)\n\njulia> sample(gdemo(1.0), ESS(), 1_000) |> mean\nMean\n\n│ Row │ parameters │ mean     │\n│     │ Symbol     │ Float64  │\n├─────┼────────────┼──────────┤\n│ 1   │ m          │ 0.824853 │\n\n\n\n\n\n","title":"Turing.Inference.ESS"},{"category":"type","location":"api/Inference/#Turing.Inference.ESSThresholdResampler","page":"Inference","text":"ESSThresholdResampler(threshold, scheme = StratifiedResampler())\n\nResample with scheme, but only when the effective sample size drops below threshold * nparticles. This is the default for SMC and PG.\n\n\n\n\n\n","title":"Turing.Inference.ESSThresholdResampler"},{"category":"type","location":"api/Inference/#Turing.Inference.Emcee","page":"Inference","text":"Emcee(n_walkers::Int, stretch_length=2.0)\n\nAffine-invariant ensemble sampling algorithm.\n\nReference\n\nForeman-Mackey, D., Hogg, D. W., Lang, D., & Goodman, J. (2013). emcee: The MCMC Hammer. Publications of the Astronomical Society of the Pacific, 125 (925), 306. https://doi.org/10.1086/670067\n\n\n\n\n\n","title":"Turing.Inference.Emcee"},{"category":"type","location":"api/Inference/#Turing.Inference.ExternalSampler","page":"Inference","text":"ExternalSampler{Unconstrained,S<:AbstractSampler,AD<:ADTypes.AbstractADType}\n\nRepresents a sampler that does not have a custom implementation of AbstractMCMC.step(rng, ::DynamicPPL.Model, spl).\n\nThe Unconstrained type-parameter is to indicate whether the sampler requires unconstrained space.\n\nFields\n\nsampler::AbstractMCMC.AbstractSampler: the sampler to wrap\nadtype::ADTypes.AbstractADType: the automatic differentiation (AD) backend to use\n\nTuring.jl's interface for external samplers\n\nIf you implement a new MySampler <: AbstractSampler and want it to work with Turing.jl models, there are two options:\n\nDirectly implement the AbstractMCMC.step methods for DynamicPPL.Model. That is to say, implement AbstractMCMC.step(rng::Random.AbstractRNG, model::DynamicPPL.Model, sampler::MySampler; kwargs...) and related methods. This is the most powerful option and is what Turing.jl's in-house samplers do. Implementing this means that you can directly call sample(model, MySampler(), N).\nImplement a generic AbstractMCMC.step method for AbstractMCMC.LogDensityModel (the same signature as above except that model::AbstractMCMC.LogDensityModel). This struct wraps an object that obeys the LogDensityProblems.jl interface, so your step implementation does not need to know anything about Turing.jl or DynamicPPL.jl. To use this with Turing.jl, you will need to wrap your sampler: sample(model, externalsampler(MySampler()), N).\n\nThis section describes the latter.\n\nMySampler must implement the following methods:\n\nAbstractMCMC.step (the main function for taking a step in MCMC sampling; this is documented in AbstractMCMC.jl). This function must return a tuple of two elements, a 'transition' and a 'state'.\nAbstractMCMC.step_warmup (optional; documented in AbstractMCMC.jl). If your sampler adapts, implement this and Turing.jl will route the num_warmup iterations through it. Samplers that do not implement it fall back to AbstractMCMC.step, as before.\nAbstractMCMC.getparams(external_state): How to extract the parameters from the state returned by your sampler (i.e., the second return value of step). For your sampler to work with Turing.jl, this function should return a Vector of parameter values. Note that this function does not need to perform any linking or unlinking; Turing.jl will take care of this for you. You should return the parameters exactly as your sampler sees them.\nAbstractMCMC.getstats(external_state): Extract sampler statistics corresponding to this iteration from the state returned by your sampler (i.e., the second return value of step). For your sampler to work with Turing.jl, this function should return a NamedTuple. If there are no statistics to return, return NamedTuple().\nNote that getstats should not include log-probabilities as these will be recalculated by Turing automatically for you.\n\nNotice that both of these functions take the state as input, not the transition. In other words, the transition is completely useless for the external sampler interface. This is in line with long-term plans for removing transitions from AbstractMCMC.jl and only using states.\n\nThere are a few more optional functions which you can implement to improve the integration with Turing.jl:\n\nAbstractMCMC.requires_unconstrained_space(::MySampler): If your sampler requires unconstrained space, you should return true. This tells Turing to perform linking on the VarInfo before evaluation, and ensures that the parameter values passed to your sampler will always be in unconstrained (Euclidean) space.\nTuring.Inference.supports_gibbs(::MySampler): If you want to disallow your sampler from a component in Turing's Gibbs sampler, you should make this evaluate to false. Note that the default is true, so you should only need to implement this in special cases.\nTuring.Inference.allow_discrete_variables(::MySampler): Return false if your sampler needs every variable to be continuous, as the gradient-based ones do. sample checks the model against this before it starts.\nTuring.Inference.gibbs_get_stats(::MyState): The statistics of the step that produced this state, as a NamedTuple, for a Gibbs chain to carry. Gibbs drops component transitions, so they cannot come from there. The wrapper implements this for you from AbstractMCMC.getstats.\nTuring.Inference.post_sample_hook(chain, ::MySampler): Anything to report once sampling has finished, such as a warning about numerical errors. Returns nothing by default.\nTuring.Inference.init_strategy(::MySampler): The DynamicPPL.AbstractInitStrategy your sampler starts from when the caller gives no initial_params. The default is InitFromPrior(); Gibbs asks each component for its own.\n\nsupports_gibbs, allow_discrete_variables, init_strategy and post_sample_hook are the whole of Turing's own interface for running a sampler on a model, together with allow_varying_dimension, which is on the list for a sampler you write yourself but is not forwarded through this wrapper, for the reason below. One that implements AbstractMCMC.step for DynamicPPL.Model (option 1 above) and overrides whichever of them do not match its defaults needs no wrapping at all; externalsampler exists only to supply the step method option 2 leaves out. Serving as a Gibbs component takes two further methods, Turing.Inference.gibbs_get_parameter_values and Turing.Inference.gibbs_update_state!!, which the wrapper implements for you – and which are also why allow_varying_dimension is not on the list above. allow_varying_dimension is not forwarded, and the wrapper implements no gibbs_update_state!! for a ReshapedBlock: the wrapped state is opaque, so there is no way to tell what within it is shaped like the block. A wrapped sampler therefore cannot own a Gibbs block whose set of variables changes, whatever it declares; one that needs to has to take option 1.\n\nOne method the wrapper cannot implement for you:\n\nAbstractMCMC.setparams!!(model::AbstractMCMC.LogDensityModel, state, params): required to serve as a Gibbs component. Gibbs re-conditions the model between sweeps, so this must recompute any log-density the state caches, not only write params into it. Define the three-argument form: AbstractMCMC's fallback drops model and calls a two-argument setparams!!(state, params), which cannot recompute anything, and a state that caches a log-density would then start each step from the previous conditioning's value.\n\n\n\n\n\n","title":"Turing.Inference.ExternalSampler"},{"category":"type","location":"api/Inference/#Turing.Inference.Gibbs","page":"Inference","text":"Gibbs\n\nA type representing a Gibbs sampler.\n\nConstructors\n\nGibbs needs to be given a set of pairs of variable names and samplers. Instead of a single variable name per sampler, one can also give an iterable of variables, all of which are sampled by the same component sampler.\n\nEach variable name can be given as either a Symbol or a VarName.\n\nSome examples of valid constructors are:\n\nGibbs(:x => NUTS(), :y => MH())\nGibbs(@varname(x) => NUTS(), @varname(y) => MH())\nGibbs((@varname(x), :y) => NUTS(), :z => MH())\n\nEvery variable in the model must be handled by at least one component sampler, and several components may sample the same variable. What they may not do is split a value that the model stores as a unit: if one component declares x and another x[1], whether Gibbs can express that depends on the model's tilde statements. Written element by element – x[1] ~ Normal(); x[2] ~ Normal() – each element is a key of its own and the partition works. Written as one draw, x ~ MvNormal(...), x is a single key: Gibbs cannot free part of it and throws. Which sampler holds the containing block makes no difference, because the values Gibbs threads come from evaluating the model rather than from what a component hands back.\n\nVariables the model only reaches on some sweeps need care:\n\n@model function f()\n    x ~ Normal()\n    y ~ Normal()\n    if x > 0\n        z ~ Normal()\n    end\nend\n\n# `x` decides whether `z` exists, so they share a block, sampled by `PG`, which can handle a\n# set of variables that changes between sweeps.\nsample(f(), Gibbs(@varname(y) => MH(), (@varname(x), @varname(z)) => PG(20)), 1000)\n\nThree partitions of that model are rejected, each naming the variable and the components:\n\nGibbs(@varname(x) => MH(), @varname(y) => MH()) – nothing samples z. Every variable the model reaches must belong to a component, whether or not it is always reached.\nGibbs(@varname(x) => MH(), @varname(y) => MH(), @varname(z) => PG(20)) – z is split from x, which decides whether it exists. The x component's step is what makes z come and go, so that step proposes between states with different sets of variables while a different component samples z, leaving that one conditioning on a z the proposed state does not have. The chain still runs but comes back biased, which is why this is refused rather than warned about. Giving z to PG does not help: it is never asked, because it is not the component whose step changed anything.\nGibbs(@varname(y) => MH(), (@varname(x), @varname(z)) => MH(@varname(z) => Normal())) – the block is right, but z is given a proposal of its own, so its proposal density no longer cancels against its prior and the acceptance ratio between the two supports is not the one the algorithm assumes. The same partition with a plain MH() samples correctly, because proposing from the prior is what makes the cancellation hold.\n\nThe same applies to a new element rather than a new name: an x[5] first reached inside a branch is treated exactly like z above.\n\nWhen each is caught varies, and cannot be made uniform. A component that cannot serve in Gibbs at all is refused when the Gibbs sampler is constructed, and anything already visible in the first draw is refused at the first step. The rest can only surface while sampling, because whether z exists depends on x, which is drawn during the run: the same partition may be refused at the first sweep or fifty sweeps in, depending on the draws. Sampling stops with an error at that point rather than carrying on, since the chain from an invalid partition is biased rather than merely noisy.\n\nA component's step can reach into another block in two ways, because a variable in one block may decide something about a variable in another without ever sampling it. Only one of the two is refused:\n\nA component may never change the dimension of a variable belonging to another component, or whether that variable exists at all, unless the two share it.\n\nSharing is the remedy: put the deciding variable and what it decides in one block, or write a component that samples both.\n\nWhat another component's step changes Effect on this block Allowed?\nwhether a tilde statement executes variable appears or leaves no\nwhich distribution a tilde draws from its family or its support moves yes, at your risk\na distribution's parameters only neither; the form is unchanged yes\n\nThe third row is the ordinary hierarchical case and carries no risk at all: under m ~ Normal(0, 1) in one block and x ~ Normal(m, 1) in another, x's distribution differs every sweep while the set it is drawn from does not.\n\nThe first row is what Gibbs refuses. The shape to recognise is a variable that decides how many of another variable exist:\n\n@model function bad_dimension()\n    b ~ Bernoulli(0.5)\n    θ = Vector{Float64}(undef, b ? 2 : 1)\n    for i in eachindex(θ)\n        θ[i] ~ Normal(0, 1)\n    end\n    return 1.0 ~ Normal(sum(θ), 1.0)\nend\n\nGibbs(@varname(b) => MH(), @varname(θ) => MH()) is refused, with\n\nThe variable θ[2] stopped existing during a step of the component sampling b, which does not\nsample it. The component sampling θ does.\n\nThe risk it is refused for: while b's component steps, θ is conditioned and cannot move, so if b flips the sweep now holds a θ of the wrong length. Whichever component then conditions on it is conditioning on a state that does not exist, and the chain comes back biased rather than merely slow. Gibbs((@varname(b), @varname(θ)) => PG(20)) samples the same model correctly – P(b=1) of 0.467, 0.486, 0.480 over three seeds – because PG redraws whatever the model reaches each sweep, which is what owning a varying set requires. Declaring that is allow_varying_dimension.\n\nwarning: A support that another block decides can silently give the wrong answer\nThe second row is permitted, and Gibbs will not stop you, but it is only correct when the supports overlap enough for the chain to move between them. Each component's kernel stays invariant for its own full conditional; what you can lose is irreducibility, and a chain that is invariant but not irreducible converges to the wrong distribution without erring.The failing shape is disjoint supports. With@model function bad()\n    b ~ Bernoulli(0.5)\n    if b\n        x ~ Uniform(0.0, 1.0)\n    else\n        x ~ Uniform(2.0, 3.0)\n    end\n    return 0.5 ~ Normal(x, 5.0)\nendand Gibbs(@varname(b) => MH(), @varname(x) => MH()), once x is in (0, 1) the b component evaluates p(b = 0 | x) = 0, because that x is impossible under Uniform(2, 3). So b can never flip, and the chain is absorbed in whichever branch it started: measured P(b=1) of 0.0, 0.0 and 1.0 over three seeds, against 0.515 from Gibbs((@varname(b), @varname(x)) => MH()) on the same model. Nothing warns, and the chain looks healthy.Note that dimension is not what makes this fatal, which is why the rule above does not reach it. x ~ Dirichlet(3) against x ~ MvNormal(zeros(3), I) has three values either way and is absorbed just as thoroughly – 0.0 on every seed tried, against 0.644 in one block. Nor is a change of family: Uniform(0, 1) and Uniform(2, 3) are the same family.What separates safe from fatal is whether every pair of reachable supports shares enough mass, and no single model evaluation can decide that, which is why this is yours to establish rather than Gibbs's to check.Nested supports are the safe shape. With x ~ Uniform(-5, 5) in one block and y ~ truncated(Normal(); lower=x) in another – Turing.jl#2801 – every reachable pair overlaps, and the split chain does explore the whole of x's range and converge to the same answer as one block over both. Expect it to mix much more slowly, though: that split needed of the order of 200,000 draws to settle where the shared block needed a fifth of that. If you are unsure, sample the same model in one block and compare.\n\nWhat is refused – a variable appearing or leaving – is checked on a best-effort basis, and that too is worth knowing precisely.\n\nwarning: The existence check is best-effort, not a guarantee\nGibbs compares the snapshots either side of a component's step, so it only ever observes states that component accepted. An MH component that proposes the deciding variable across and has the proposal rejected leaves no trace, and the sweep carries on.In practice it is reliable, because the deciding variable usually does move: on the model above it refused the split partition on all 40 seeds tried, at 50, 300 and 2000 draws alike. But that is a fact about those chains, not a guarantee – a decider that happens to stay put for the whole run leaves the partition unexamined. A run that completes is not evidence that the partition is valid.\n\nnote: Note\nNone of this is a property of Gibbs sampling. A component's target is its full conditional whichever block the decider sits in. What the existence rule protects is this implementation's bookkeeping: a component holding a flat parameter layout cannot be re-pointed at a different one mid-step, and PG/CSMC are the only components here that rebuild their trace each sweep.A change of support that leaves the dimension alone can still move a block's linked dimension – a simplex occupies one number fewer than the free vector of the same length – and that is refused for a component which cannot rebuild, by gibbs_update_state!! for a ReshapedBlock. That refusal is about layout, not about correctness of the chain.\n\nEach component sampler initialises the variables it samples with its own default strategy, so e.g. an HMC component starts from its InitFromUniform. A user-supplied initial_params overrides that and applies to the model as a whole; it cannot yet be set per component.\n\nFields\n\nvarnames::NTuple{N, AbstractVector{<:AbstractPPL.VarName}} where N: varnames representing variables for each sampler\nsamplers::NTuple{N, Any} where N: samplers for each entry in varnames\n\n\n\n\n\n","title":"Turing.Inference.Gibbs"},{"category":"type","location":"api/Inference/#Turing.Inference.GibbsConditional","page":"Inference","text":"GibbsConditional(get_cond_dists)\n\nA Gibbs component sampler that samples variables according to user-provided analytical conditional posterior distributions.\n\nWhen using Gibbs sampling, sometimes one may know the analytical form of the posterior for a given variable, given the conditioned values of the other variables. In such cases one can use GibbsConditional as a component sampler to to sample from these known conditionals directly, avoiding any MCMC methods. One does so with\n\nsampler = Gibbs(\n    (@varname(var1), @varname(var2)) => GibbsConditional(get_cond_dists),\n    other samplers go here...\n)\n\nHere get_cond_dists(vnt::VarNamedTuple) should be a function that takes a VarNamedTuple that contains the values of all other variables (apart from var1 and var2), and returns the conditional posterior distributions for var1 and var2.\n\nVarNamedTuples behave very similarly to Dict{VarName,Any}s, but are more efficient and more general: you can obtain values simply by using, e.g. vnt[@varname(var3)]. See https://turinglang.org/docs/usage/varnamedtuple/ for more details on VarNamedTuples.\n\nYou may, of course, have any number of variables being sampled as a block in this manner, we only use two as an example.\n\nThe return value of get_cond_dists(vnt) should be one of the following:\n\nA single Distribution, if only one variable is being sampled.\nA VarNamedTuple of Distributions, which represents a mapping from variable names to their conditional posteriors. Please see the documentation linked above for information on how to construct VarNamedTuples.\n\nFor convenience, we also allow the following return values (which are internally converted into a VarNamedTuple):\n\nA NamedTuple of Distributions, which is like the AbstractDict case but can be used if all the variable names are single Symbols, e.g.: (; var1=dist1, var2=dist2).\nAn AbstractDict{<:VarName,<:Distribution} that maps the variables being sampled to their conditional posteriors E.g. Dict(@varname(var1) => dist1, @varname(var2) => dist2).\n\nNote that the AbstractDict case is likely to incur a performance penalty; we recommend using VarNamedTuples directly.\n\nExamples\n\nusing Turing\n\n# Define a model\n@model function inverse_gdemo(x)\n    precision ~ Gamma(2, inv(3))\n    std = sqrt(1 / precision)\n    m ~ Normal(0, std)\n    for i in eachindex(x)\n        x[i] ~ Normal(m, std)\n    end\nend\n\n# Define analytical conditionals. See\n# https://en.wikipedia.org/wiki/Conjugate_prior#When_likelihood_function_is_a_continuous_distribution\nfunction cond_precision(vnt)\n    a = 2.0\n    b = 3.0\n    m = vnt[@varname(m)]\n    x = vnt[@varname(x)]\n    n = length(x)\n    a_new = a + (n + 1) / 2\n    b_new = b + sum(abs2, x .- m) / 2 + m^2 / 2\n    return Gamma(a_new, 1 / b_new)\nend\n\nfunction cond_m(vnt)\n    precision = vnt[@varname(precision)]\n    x = vnt[@varname(x)]\n    n = length(x)\n    m_mean = sum(x) / (n + 1)\n    m_var = 1 / (precision * (n + 1))\n    return Normal(m_mean, sqrt(m_var))\nend\n\n# Sample using GibbsConditional\nmodel = inverse_gdemo([1.0, 2.0, 3.0])\nchain = sample(model, Gibbs(\n    :precision => GibbsConditional(cond_precision),\n    :m => GibbsConditional(cond_m)\n), 1000)\n\n\n\n\n\n","title":"Turing.Inference.GibbsConditional"},{"category":"type","location":"api/Inference/#Turing.Inference.GibbsInitStrategy","page":"Inference","text":"GibbsInitStrategy(varnames, strategies)\n\nInitialise each variable with the strategy of the component sampler that samples it, choosing by ownership; a variable no component claims falls back to the prior.\n\nThis produces the single initial draw the first sweep starts from, and nothing else. Each component's own initialisation does not go through it: gibbs_initialstep_recursive asks init_strategy(sampler) directly, because ownership is ambiguous where components overlap. So an HMC component gets its InitFromUniform starting point from there, not from here.\n\n\n\n\n\n","title":"Turing.Inference.GibbsInitStrategy"},{"category":"type","location":"api/Inference/#Turing.Inference.HMC","page":"Inference","text":"HMC(ϵ::Float64, n_leapfrog::Int; adtype::ADTypes.AbstractADType = AutoForwardDiff())\n\nHamiltonian Monte Carlo sampler with static trajectory.\n\nArguments\n\nϵ: The leapfrog step size to use.\nn_leapfrog: The number of leapfrog steps to use.\nadtype: The automatic differentiation (AD) backend.   If not specified, ForwardDiff is used, with its chunksize automatically determined.\n\nUsage\n\nHMC(0.05, 10)\n\nTips\n\nIf you are receiving gradient errors when using HMC, try reducing the leapfrog step size ϵ, e.g.\n\n# Original step size\nsample(gdemo([1.5, 2]), HMC(0.1, 10), 1000)\n\n# Reduced step size\nsample(gdemo([1.5, 2]), HMC(0.01, 10), 1000)\n\n\n\n\n\n","title":"Turing.Inference.HMC"},{"category":"type","location":"api/Inference/#Turing.Inference.HMCDA","page":"Inference","text":"HMCDA(\n    n_adapts::Int, δ::Float64, λ::Float64; ϵ::Float64 = 0.0;\n    adtype::ADTypes.AbstractADType = AutoForwardDiff(),\n)\n\nHamiltonian Monte Carlo sampler with Dual Averaging algorithm.\n\nUsage\n\nHMCDA(200, 0.65, 0.3)\n\nArguments\n\nn_adapts: Numbers of samples to use for adaptation.\nδ: Target acceptance rate. 65% is often recommended.\nλ: Target leapfrog length.\nϵ: Initial step size; 0 means automatically search by Turing.\nadtype: The automatic differentiation (AD) backend.   If not specified, ForwardDiff is used, with its chunksize automatically determined.\n\nReference\n\nFor more information, please view the following paper (arXiv link):\n\nHoffman, Matthew D., and Andrew Gelman. \"The No-U-turn sampler: adaptively setting path lengths in Hamiltonian Monte Carlo.\" Journal of Machine Learning Research 15, no. 1 (2014): 1593-1623.\n\n\n\n\n\n","title":"Turing.Inference.HMCDA"},{"category":"type","location":"api/Inference/#Turing.Inference.InitFromProposals","page":"Inference","text":"InitFromProposals(proposals::VarNamedTuple, verbose::Bool)\n\nAn initialisation strategy that samples variables from user-defined proposal distributions. If a proposal distribution is not found in proposals, then we defer to sampling from the prior.\n\n\n\n\n\n","title":"Turing.Inference.InitFromProposals"},{"category":"type","location":"api/Inference/#Turing.Inference.LinkedRW","page":"Inference","text":"LinkedRW(cov_matrix)\n\nDefine a random-walk proposal in linked space with the given covariance matrix. Note that the size of the covariance matrix must correspond exactly to the size of the variable in linked space.\n\nLinkedRW(variance::Real)\n\nIf a Real variance is provided, LinkedRW will just generate a covariance matrix of variance * LinearAlgebra.I.\n\n\n\n\n\n","title":"Turing.Inference.LinkedRW"},{"category":"type","location":"api/Inference/#Turing.Inference.MH","page":"Inference","text":"MH(vn1 => proposal1, vn2 => proposal2, ...)\n\nConstruct a Metropolis-Hastings algorithm.\n\nEach argument proposal can be\n\nBlank (i.e. MH()), in which case MH defaults to using the prior for each parameter as the proposal distribution.\nA mapping of VarNames to a Distribution, LinkedRW, or a generic callable that defines a conditional proposal distribution.\n\nMH(cov_matrix)\n\nConstruct a Metropolis-Hastings algorithm that performs random-walk sampling in linked space, with proposals drawn from a multivariate normal distribution with the given covariance matrix.\n\nExamples\n\nConsider the model below:\n\n@model function gdemo()\n    s ~ InverseGamma(2,3)\n    m ~ Normal(0, sqrt(s))\n    1.5 ~ Normal(m, sqrt(s))\n    2.0 ~ Normal(m, sqrt(s))\nend\n\nThe default constructor, MH(), uses the prior distributions as proposals. So, new proposals are obtained by sampling s from InverseGamma(2,3) and m from Normal(0, sqrt(s)).\n\nspl = MH()\n\nAlternatively, a mapping of variable names to proposal distributions can be provided. This implies the use of static proposals for each variable. If a variable is not specified, its prior distribution is used as the proposal.\n\n# Use a static proposal for s (which happens to be the same as the prior) and a static\n# proposal for m (note that this isn't a random walk proposal).\nspl = MH(\n    # This happens to be the same as the prior\n    @varname(s) => InverseGamma(2, 3),\n    # This is different from the prior\n    @varname(m) => Normal(0, 1),\n)\n\nIf the VarName of interest is a single symbol, you can also use a Symbol instead.\n\nspl = MH(\n    :s => InverseGamma(2, 3),\n    :m => Normal(0, 1),\n)\n\nYou can also use a callable to define a proposal that is conditional on the current values. The callable must accept a single argument, which is a DynamicPPL.VarNamedTuple that holds all the values of the parameters from the previous step. You can obtain the value of a specific parameter by indexing into this VarNamedTuple using a VarName (note that symbol indexing is not supported). The callable must then return a Distribution from which to draw the proposal.\n\nnote: Note\nIn general, there is no way for Turing to reliably detect whether a proposal is meant to be a callable or not, since callable structs may have any type. Hence, any proposal that is not a distribution is assumed to be a callable.\n\nspl = MH(\n    # This is a static proposal (same as above).\n    @varname(s) => InverseGamma(2, 3),\n    # This is a conditional proposal, which proposes m from a normal\n    # distribution centred at the current value of m, with a standard\n    # deviation of 0.5.\n    @varname(m) => (vnt -> Normal(vnt[@varname(m)], 0.5)),\n)\n\nNote that when using conditional proposals, the values obtained by indexing into the VarNamedTuple are always in untransformed space, which are constrained to the support of the distribution. Sometimes, you may want to define a random-walk proposal in unconstrained (i.e. 'linked') space. For this, you can use LinkedRW as a proposal, which takes a covariance matrix as an argument:\n\nusing LinearAlgebra: Diagonal\nspl = MH(\n    @varname(s) => InverseGamma(2, 3),\n    @varname(m) => LinkedRW(Diagonal([0.25]))\n)\n\nIn the above example, LinkedRW(Diagonal([0.25])) defines a random-walk proposal for m in linked space. This is in fact the same as the conditional proposal above, because m is already unconstrained, and so the unconstraining transformation is the identity.\n\nHowever, s is constrained to be positive, and so using a LinkedRW proposal for s would be different from using a normal proposal in untransformed space (LinkedRW will ensure that the proposals for s always remain positive in untransformed space).\n\nspl = MH(\n    @varname(s) => LinkedRW(Diagonal([0.5])),\n    @varname(m) => LinkedRW(Diagonal([0.25])),\n)\n\nFinally, providing just a single covariance matrix will cause MH to perform random-walk sampling in linked space with proposals drawn from a multivariate normal distribution. All variables are linked in this case. The provided matrix must be positive semi-definite and square. This example is therefore equivalent to the previous one:\n\n# Providing a custom variance-covariance matrix\nspl = MH(\n    [0.50 0;\n     0 0.25]\n)\n\n\n\n\n\n","title":"Turing.Inference.MH"},{"category":"type","location":"api/Inference/#Turing.Inference.MHState","page":"Inference","text":"MHState(vi, stat)\n\nThe MH sampler state: the varinfo of the current draw, plus the statistics of the step that produced it. The statistics are kept on the state because Gibbs drops component transitions, which is where they would otherwise live (see gibbs_get_stats).\n\n\n\n\n\n","title":"Turing.Inference.MHState"},{"category":"type","location":"api/Inference/#Turing.Inference.MultinomialResampler","page":"Inference","text":"Multinomial resampling: n independent draws from the categorical over weights.\n\n\n\n\n\n","title":"Turing.Inference.MultinomialResampler"},{"category":"type","location":"api/Inference/#Turing.Inference.NUTS","page":"Inference","text":"NUTS(n_adapts::Int, δ::Float64; max_depth::Int=10, Δ_max::Float64=1000.0, init_ϵ::Float64=0.0, adtype::ADTypes.AbstractADType=AutoForwardDiff())\n\nNo-U-Turn Sampler (NUTS) sampler.\n\nUsage\n\nNUTS()            # Use default NUTS configuration.\nNUTS(1000, 0.65)  # Use 1000 adaption steps, and target accept ratio 0.65.\n\nArguments\n\nn_adapts::Int: The number of samples to use with adaptation.\nδ::Float64: Target acceptance rate for dual averaging.\nmax_depth::Int: Maximum doubling tree depth.\nΔ_max::Float64: Maximum divergence during doubling tree.\ninit_ϵ::Float64: Initial step size; 0 means automatically searching using a heuristic procedure.\nadtype::ADTypes.AbstractADType: The automatic differentiation (AD) backend.   If not specified, ForwardDiff is used, with its chunksize automatically determined.\n\n\n\n\n\n","title":"Turing.Inference.NUTS"},{"category":"type","location":"api/Inference/#Turing.Inference.PG","page":"Inference","text":"struct PG{R<:Turing.Inference.AbstractResampler} <: Turing.Inference.ParticleInference\n\nParticle Gibbs (conditional SMC) sampler.\n\nFields\n\nnparticles::Int64: number of particles\nresampler::Turing.Inference.AbstractResampler: resampling scheme\nmultithreaded::Bool: reweight the particles across threads within each sweep\n\n\n\n\n\n","title":"Turing.Inference.PG"},{"category":"method","location":"api/Inference/#Turing.Inference.PG-Tuple{Integer}","page":"Inference","text":"PG(n, [resampler = ESSThresholdResampler(0.5)]; multithreaded = false)\nPG(n, [scheme = StratifiedResampler(), ]threshold; multithreaded = false)\n\nParticle Gibbs sampler with n particles. By default resampling is triggered whenever the effective sample size drops below half the number of particles. The selected scheme applies to the unconditional first sweep only; conditional sweeps draw their ancestors from the categorical over the weights, for the reason given in the resampling-schemes section of this file.\n\nSet multithreaded = true to evaluate the particles across threads within each sweep; results are unchanged (start Julia with multiple threads, e.g. julia -t auto, for this to have effect). Threads are the only option here: a suspended particle is a live Libtask task and cannot be serialised, so a single sweep cannot be spread across processes. Passing MCMCThreads() or MCMCDistributed() to sample parallelises whole chains instead, which is a separate axis and composes with this one.\n\nwarning: `log_normalizing_constant` is biased for PG\nPG chains carry log_normalizing_constant, but its exponential is not an unbiased estimator of p(y) and must not be used for model comparison. A conditional sweep retains the reference whatever its weight. Because the reference is a posterior draw rather than a proposal draw, it usually has much higher likelihood than a fresh particle and inflates the mean weight at each step. In a linear Gaussian SSM with known p(y), E[Ẑ] exceeded p(y) by 80% at n = 16 and 16% at n = 64; the bias decreased approximately as 1/n in that experiment but remained substantial at these particle counts. Use SMC when an unbiased estimator of p(y) is required. Its likelihood-scale estimate exp(log_normalizing_constant) is unbiased under the usual particle-filter assumptions.\n\n\n\n\n\n","title":"Turing.Inference.PG"},{"category":"type","location":"api/Inference/#Turing.Inference.PGState","page":"Inference","text":"PGState(trajectory)\n\nParticle Gibbs sampler state: the retained trajectory's raw values, which the next sweep's reference particle reuses. Plain data, because sampler state has to survive save_state=true and MCMCDistributed(), whereas the Particle it is read off owns a live Libtask.TapedTask that cannot be serialised. Nothing else needs carrying over: the reference consumes no randomness of its own, and every other particle is seeded from the sampler's rng.\n\n\n\n\n\n","title":"Turing.Inference.PGState"},{"category":"type","location":"api/Inference/#Turing.Inference.Particle","page":"Inference","text":"Particle(model, rng)\nParticle(model, rng, reference::DynamicPPL.VarNamedTuple)\n\nA single particle: a suspended model execution together with its varinfo, its own rng, and an accumulated logweight.\n\nWithout reference the particle draws from the prior. Given a retained trajectory's raw values it becomes a conditional-SMC reference pinned to that trajectory, erroring if its execution reaches an address that trajectory lacks or finishes without reaching one it has.\n\n\n\n\n\n","title":"Turing.Inference.Particle"},{"category":"method","location":"api/Inference/#Turing.Inference.PolynomialStepsize-Union{Tuple{T}, Tuple{T, T, T}} where T<:Real","page":"Inference","text":"PolynomialStepsize(a[, b=0, γ=0.55])\n\nCreate a polynomially decaying stepsize function.\n\nAt iteration t, the step size is\n\na (b + t)^-γ\n\n\n\n\n\n","title":"Turing.Inference.PolynomialStepsize"},{"category":"type","location":"api/Inference/#Turing.Inference.Prior","page":"Inference","text":"Prior()\n\nAlgorithm for sampling from the prior.\n\n\n\n\n\n","title":"Turing.Inference.Prior"},{"category":"type","location":"api/Inference/#Turing.Inference.ProduceLogLikelihoodAccumulator","page":"Inference","text":"ProduceLogLikelihoodAccumulator{T} <: LogProbAccumulator{T}\n\nA likelihood accumulator that Libtask.produces each increment as it accumulates it, which is what turns a model evaluation into a particle filter: one produce per likelihood term, so the sweep sees one filtering step per observe. Substituting it for LogLikelihoodAccumulator is the only thing that distinguishes a particle's varinfo.\n\n\n\n\n\n","title":"Turing.Inference.ProduceLogLikelihoodAccumulator"},{"category":"type","location":"api/Inference/#Turing.Inference.RepeatSampler","page":"Inference","text":"RepeatSampler <: AbstractMCMC.AbstractSampler\n\nA RepeatSampler is a container for a sampler and a number of times to repeat it.\n\nFields\n\nsampler: The sampler to repeat\nnum_repeat: The number of times to repeat the sampler\n\nExamples\n\nrepeated_sampler = RepeatSampler(sampler, 10)\n# The initial step is a single step of `sampler`; it is the steps from a state that repeat.\n_, state = AbstractMCMC.step(rng, model, repeated_sampler)\nAbstractMCMC.step(rng, model, repeated_sampler, state) # take 10 steps of `sampler`\n\n\n\n\n\n","title":"Turing.Inference.RepeatSampler"},{"category":"type","location":"api/Inference/#Turing.Inference.ReshapedBlock","page":"Inference","text":"ReshapedBlock(variable::VarName, change::Symbol)\n\nSays that the parameter layout of the block a Gibbs component samples is no longer the one that component last saw, because of variable. change says how:\n\n:joined, the variable is in the block now and was not before;\n:left, the reverse;\n:respecified, it is in both, but its tilde statement now draws from a distribution with a different linking transform, so the block's linked dimension may have moved although its leaves have not.\n\nAll three mean the same thing to a sampler – rebuild whatever you sized for the block – and change only shapes the error message when one cannot.\n\nDefined here rather than beside the rest of the Gibbs interface because the samplers that dispatch on it are loaded first. See the five-argument gibbs_update_state!! for what it is for.\n\n\n\n\n\n","title":"Turing.Inference.ReshapedBlock"},{"category":"type","location":"api/Inference/#Turing.Inference.SGHMC","page":"Inference","text":"SGHMC{AD}\n\nStochastic Gradient Hamiltonian Monte Carlo (SGHMC) sampler.\n\nFields\n\nlearning_rate::Real\nmomentum_decay::Real\nadtype::Any\n\nReference\n\nTianqi Chen, Emily Fox, & Carlos Guestrin (2014). Stochastic Gradient Hamiltonian Monte Carlo. In: Proceedings of the 31st International Conference on Machine Learning (pp. 1683–1691).\n\n\n\n\n\n","title":"Turing.Inference.SGHMC"},{"category":"method","location":"api/Inference/#Turing.Inference.SGHMC-Tuple{}","page":"Inference","text":"SGHMC(;\n    learning_rate::Real,\n    momentum_decay::Real,\n    adtype::ADTypes.AbstractADType = AutoForwardDiff(),\n)\n\nCreate a Stochastic Gradient Hamiltonian Monte Carlo (SGHMC) sampler.\n\nIf the automatic differentiation (AD) backend adtype is not provided, ForwardDiff with automatically determined chunksize is used.\n\nReference\n\nTianqi Chen, Emily Fox, & Carlos Guestrin (2014). Stochastic Gradient Hamiltonian Monte Carlo. In: Proceedings of the 31st International Conference on Machine Learning (pp. 1683–1691).\n\n\n\n\n\n","title":"Turing.Inference.SGHMC"},{"category":"type","location":"api/Inference/#Turing.Inference.SGLD","page":"Inference","text":"SGLD\n\nStochastic gradient Langevin dynamics (SGLD) sampler.\n\nFields\n\nstepsize::Any: Step size function.\nadtype::Any\n\nReference\n\nMax Welling & Yee Whye Teh (2011). Bayesian Learning via Stochastic Gradient Langevin Dynamics. In: Proceedings of the 28th International Conference on Machine Learning (pp. 681–688).\n\n\n\n\n\n","title":"Turing.Inference.SGLD"},{"category":"method","location":"api/Inference/#Turing.Inference.SGLD-Tuple{}","page":"Inference","text":"SGLD(;\n    stepsize = PolynomialStepsize(0.01),\n    adtype::ADTypes.AbstractADType = AutoForwardDiff(),\n)\n\nStochastic gradient Langevin dynamics (SGLD) sampler.\n\nBy default, a polynomially decaying stepsize is used.\n\nIf the automatic differentiation (AD) backend adtype is not provided, ForwardDiff with automatically determined chunksize is used.\n\nReference\n\nMax Welling & Yee Whye Teh (2011). Bayesian Learning via Stochastic Gradient Langevin Dynamics. In: Proceedings of the 28th International Conference on Machine Learning (pp. 681–688).\n\nSee also: PolynomialStepsize\n\n\n\n\n\n","title":"Turing.Inference.SGLD"},{"category":"type","location":"api/Inference/#Turing.Inference.SMC","page":"Inference","text":"struct SMC{R<:Turing.Inference.AbstractResampler} <: Turing.Inference.ParticleInference\n\nSequential Monte Carlo sampler.\n\nFields\n\nresampler::Turing.Inference.AbstractResampler: resampling scheme\nmultithreaded::Bool: reweight the particles across threads within each sweep\n\n\n\n\n\n","title":"Turing.Inference.SMC"},{"category":"method","location":"api/Inference/#Turing.Inference.SMC-Tuple{}","page":"Inference","text":"SMC([resampler = ESSThresholdResampler(0.5)]; multithreaded = false)\nSMC([scheme = StratifiedResampler(), ]threshold; multithreaded = false)\n\nSequential Monte Carlo sampler. By default stratified resampling is triggered whenever the effective sample size drops below half the number of particles.\n\nSet multithreaded = true to evaluate the particles across threads within each sweep; results are unchanged (start Julia with multiple threads, e.g. julia -t auto, for this to have effect). Threads are the only option here: a suspended particle is a live Libtask task and cannot be serialised, so a single sweep cannot be spread across processes. Passing MCMCThreads() or MCMCDistributed() to sample parallelises whole chains instead, which is a separate axis and composes with this one.\n\nThe resampling scheme types (StratifiedResampler, SystematicResampler, MultinomialResampler, ESSThresholdResampler) are not exported; refer to them as e.g. Turing.Inference.SystematicResampler.\n\n\n\n\n\n","title":"Turing.Inference.SMC"},{"category":"type","location":"api/Inference/#Turing.Inference.SMCContext","page":"Inference","text":"SMCContext\n\nLeaf context marking a model evaluation as a particle-filter step: tilde_assume!! draws from the prior using the particle's own generator – or, for a conditional-SMC reference, reuses the retained trajectory's value at that address – and tilde_observe!! scores the observation and Libtask.produces the increment as the particle's weight.\n\n\n\n\n\n","title":"Turing.Inference.SMCContext"},{"category":"type","location":"api/Inference/#Turing.Inference.Snapshot","page":"Inference","text":"Snapshot(values, layouts)\n\nThe values Gibbs threads through a sweep, with the linked layout of each tilde statement that produced them.\n\nBoth halves come from one model evaluation and reached_values is the only thing that builds one, so block_fingerprint can compare a block's layout either side of a step without evaluating the model again.\n\n\n\n\n\n","title":"Turing.Inference.Snapshot"},{"category":"type","location":"api/Inference/#Turing.Inference.StratifiedResampler","page":"Inference","text":"Stratified resampling: one independent uniform per stratum of width 1/n.\n\n\n\n\n\n","title":"Turing.Inference.StratifiedResampler"},{"category":"type","location":"api/Inference/#Turing.Inference.SystematicResampler","page":"Inference","text":"Systematic resampling: one shared uniform placed on a regular grid of n points.\n\n\n\n\n\n","title":"Turing.Inference.SystematicResampler"},{"category":"method","location":"api/Inference/#Turing.Inference._convert_initial_params-Tuple{DynamicPPL.AbstractInitStrategy}","page":"Inference","text":"_convert_initial_params(initial_params)\n\nConvert initial_params to a DynamicPPl.AbstractInitStrategy if it is not already one, or throw a useful error message.\n\n\n\n\n\n","title":"Turing.Inference._convert_initial_params"},{"category":"method","location":"api/Inference/#Turing.Inference._default_parameter_values-Tuple{Any}","page":"Inference","text":"Turing.Inference._default_parameter_values(state)\n\nThe answer for a state with no gibbs_get_parameter_values method of its own.\n\nAn AbstractVarInfo carries its ~ values in a RawValueAccumulator, so there is one; any other state has to say for itself.\n\n\n\n\n\n","title":"Turing.Inference._default_parameter_values"},{"category":"method","location":"api/Inference/#Turing.Inference._require_owned-Tuple{Gibbs, Any, Any, Any, String}","page":"Inference","text":"_require_owned(spl, sampler, varnames, leaf, what)\n\nThrow unless leaf, whose existence just changed, is sampled by the component that changed it.\n\nA variable coming or going under one component's proposal while another samples it means that other component conditions on a variable absent from the state being proposed, and the chain comes back biased. what says which direction it moved.\n\n\n\n\n\n","title":"Turing.Inference._require_owned"},{"category":"method","location":"api/Inference/#Turing.Inference._require_same_variables-Tuple{MH, DynamicPPL.AbstractVarInfo, DynamicPPL.AbstractVarInfo}","page":"Inference","text":"_require_same_variables(spl, old_vi, new_vi)\n\nThrow if a proposal reached a different set of variables from the state it was proposed from and the variable that came or went is one spl proposes for explicitly.\n\nA default MH() proposes every variable from its prior, and then the acceptance ratio\n\np(x')L(x') q(x|x') / (p(x)L(x) q(x'|x))\n\nhas q = p, so the prior densities cancel and it collapses to L(x')/L(x). That is defined between spaces of different dimension, which makes a prior-proposal move across supports a legitimate one: on the dynamic model in the tests it recovers P(b=1) = 0.3965 over six seeds against an exact 0.394. So this is not a property of MH as a sampler, and it cannot be read off allow_varying_dimension alone.\n\nA variable with its own proposal is different: q no longer cancels against p, and the ratio across two supports is not the one the algorithm assumes. Only those are refused, and the refusal happens here rather than downstream because it has to happen whatever the outcome. On rejection MH returns the state it proposed from, so nothing afterwards can tell that a crossing was proposed, and Gibbs – which sees only the returned state – would let a run finish while every such proposal had been scored with an invalid ratio.\n\n:= quantities are excluded: they are not variables, and a branch may compute different ones without the support having changed. Membership in vns_with_proposal is tested exactly as MHUnspecifiedPriorsAccumulator tests it, so that this agrees with which variables the sampler really proposes from the prior.\n\n\n\n\n\n","title":"Turing.Inference._require_same_variables"},{"category":"method","location":"api/Inference/#Turing.Inference._reshape_description-Tuple{Turing.Inference.ReshapedBlock}","page":"Inference","text":"Turing.Inference.gibbs_update_state!!(\n    sampler::AbstractSampler, state, model::Model, global_vals::VarNamedTuple,\n    reshaped::ReshapedBlock\n)\n\nUpdate the state of a Gibbs component sampler whose block now holds a different set of variables from the one it last stepped.\n\nThat happens when another component samples the variable deciding whether one of this block's variables exists:\n\n@model function f(y)\n    b ~ Bernoulli(0.3)\n    θ = zeros(2)\n    θ[1] ~ Normal()\n    b == 1 && (θ[2] ~ Normal())\n    return y ~ Normal(sum(θ), 0.5)\nend\n\nGibbs((@varname(b), @varname(θ)) => PG(20), @varname(θ) => HMC(0.1, 5))\n\nPG samples b and θ together, then HMC samples θ again. When PG's step flips b, θ[2] appears or vanishes, so by the time HMC next steps its block has a different shape from the one it last saw. Each step still sees one fixed shape, so the scheme is valid; HMC only has to rebuild the parameter layout and phasepoint it cached for the old shape. Without a flip, θ never changes shape and the case never arises.\n\nDefaults to throwing, because most components carry something so sized: an adapted mass matrix, a flat parameter layout, a set of prior means. Implementing this method is how a sampler says that it copes – the implementation is the declaration, so there is no separate trait that could fall out of step with what the sampler actually does, and a sampler written before this method existed keeps the safe answer.\n\nTuring's own answers: MH, PG and GibbsConditional delegate to the four-argument form, caching nothing shaped like the block. A Hamiltonian that is not adapting rebuilds both the parameter layout and the phasepoint, which covers HMC and also NUTS(0, δ) and HMCDA(0, δ, λ), whose states carry NoAdaptation. An adapting NUTS or HMCDA does not implement it, since gen_metric renews an AdaptiveHamiltonian's metric from state.adaptor, whose mass matrix is sized for the shape it adapted to; resetting the adaptation instead would discard it mid-chain, which is a decision for the user rather than a detail of conditioning. Nor does ESS, whose prior means are gathered for the block it was built on, nor externalsampler, whose wrapped state is opaque.\n\n\n\n\n\n","title":"Turing.Inference._reshape_description"},{"category":"method","location":"api/Inference/#Turing.Inference._same_layout-Tuple{Any, Any}","page":"Inference","text":"_same_layout(before, now)\n\nWhether two (dist, val) records occupy the same number of numbers once linked.\n\nAn unchanged distribution settles it without measuring: the linked width is a function of the distribution, and measuring means deriving the linking transform, which under fix_transforms the caller has asked not to pay for once per tilde per step.\n\nWhen they differ the width is measured by linking the value, not inferred from the family or the bijector's type. Those are proxies that diverge from the layout: keying on the family refused an adapting NUTS for a Normal()/TDist(3) branch whose block had not moved.\n\n\n\n\n\n","title":"Turing.Inference._same_layout"},{"category":"method","location":"api/Inference/#Turing.Inference._shape_template-Tuple{UnivariateDistribution}","page":"Inference","text":"_shape_template(dist)\n\nA container shaped and typed like dist, for templated_setindex!! to size an array by, or NoTemplate() when the distribution says nothing about a shape. Only its shape and element type are read, so it is left uninitialised.\n\nThe shape has to come from the distribution being written, not from the values the state holds: those describe the previous step, and another component may have changed the block's dimension since, in which case templating a two-element result onto a one-element hint throws a BoundsError from inside the setindex.\n\nNoTemplate() alone is not enough either. A VarName carrying a Colon – m[:] ~ MvNormal(...) – gives the setindex nothing to infer a size from and it refuses outright. A multivariate or matrix-variate distribution knows its own size, which is exactly the missing hint; a univariate one at an indexed VarName does not, and does not need it.\n\n\n\n\n\n","title":"Turing.Inference._shape_template"},{"category":"method","location":"api/Inference/#Turing.Inference.advance!-Tuple{Turing.Inference.Particle}","page":"Inference","text":"advance!(particle) -> Union{Real,Nothing}\n\nRun the particle to its next observe, returning the incremental log-likelihood, or nothing once the model finishes.\n\n\n\n\n\n","title":"Turing.Inference.advance!"},{"category":"method","location":"api/Inference/#Turing.Inference.allow_discrete_variables-Tuple{AbstractMCMC.AbstractSampler}","page":"Inference","text":"Turing.Inference.allow_discrete_variables(spl::AbstractSampler)\n\nWhether spl can sample a model with discrete variables.\n\nDefaults to true. Gradient-based samplers override it to false, and sample checks the model against it before starting.\n\n\n\n\n\n","title":"Turing.Inference.allow_discrete_variables"},{"category":"method","location":"api/Inference/#Turing.Inference.allow_varying_dimension-Tuple{AbstractMCMC.AbstractSampler}","page":"Inference","text":"Turing.Inference.allow_varying_dimension(spl::AbstractSampler)\n\nWhether spl can move between supports within one of its own steps, that is, sample a block whose set of variables its own proposal changes.\n\nDefaults to false, because proposing between two supports takes a construction built for it. A LogDensityFunction's layout is fixed for the whole of a step, so it has no slot for a variable the proposal reaches part-way through: a leapfrog step that crosses into another branch raises KeyError from DynamicPPL rather than reaching this check at all. PG and CSMC rebuild their trace each sweep, drawing whatever the model reaches, so they can.\n\nMH answers true, but not because every crossing is safe for it. Whether one is depends on the variable that moved: drawn from its prior, the proposal density cancels against the prior and the ratio collapses to a likelihood ratio, which is defined across dimensions; proposed from, it does not cancel. That is a fact about one variable in one evaluation, which a trait asked once cannot supply, so MH answers true here and refuses the invalid crossings itself. A component whose answer really is uniform should give it here.\n\nThis is a different question from being handed a block at a new shape between one's own steps, which another component's step can do and which a component answers by implementing gibbs_update_state!! for a ReshapedBlock.\n\nReturning true is a claim about the algorithm, and it carries an obligation: the component must have coherent semantics for a variable it samples going away and later coming back. PG and CSMC do. The reference particle replays the retained trajectory's values, so it reproduces that execution exactly and cannot reach an address the trajectory lacks, which it would otherwise refuse; the remaining particles draw from the prior and are free to reach a different set of addresses, which is what lets the block move between supports at all.\n\nReturning true is not by itself enough. For an array whose length varies (for i in 1:n; x[i] ~ ...; end under a random n), the block has to hold n as well: with n in another component, the conditioned x[i] become observations whose number depends on it, and PG refuses with \"the number of observations must not be random\".\n\n\n\n\n\n","title":"Turing.Inference.allow_varying_dimension"},{"category":"method","location":"api/Inference/#Turing.Inference.block_fingerprint-Tuple{Turing.Inference.Snapshot, Any}","page":"Inference","text":"block_fingerprint(snapshot, varnames)\n\nThe shape of a component's block: the leaves it holds, and the linked width of each tilde statement that produced them.\n\nLeaves alone are not enough. A tilde statement that changes which distribution it draws from can keep its leaf count and still move the block's linked dimension – x ~ Dirichlet(3) occupies two numbers and x ~ MvNormal(zeros(3), I) three, both with three leaves – and a component holding a parameter layout has to be told.\n\nOnly the width, so a block whose distributions change without moving it is not a reshape: x ~ truncated(Normal(); lower=a) with a in another block is relinked every sweep and still occupies one number, and refusing that would rule out a partition that samples perfectly well.\n\n\n\n\n\n","title":"Turing.Inference.block_fingerprint"},{"category":"method","location":"api/Inference/#Turing.Inference.build_values_vnt-Tuple{DynamicPPL.Model}","page":"Inference","text":"build_values_vnt(model::DynamicPPL.Model)\n\nBuild a VarNamedTuple of the values of every variable this component conditions on: those supplied as model arguments, and those Gibbs, the user, or fix conditioned.\n\nmerge is right-biased and replaces a whole key, so an array argument with a missing element – whose other elements are observations and whose missing one Gibbs conditions on the current draw – loses its observations to the partially-set conditioned value. Those elements are put back afterwards, rather than merging leaf by leaf throughout, so that a value stored under one key stays under one key: get_cond_dists sees these keys.\n\nA missing in the context is put back the same way. condition(model; y=missing) leaves y absent as far as model execution is concerned, so the model uses the argument it was given, and get_cond_dists must see that argument rather than the missing the merge would otherwise leave in its place.\n\n\n\n\n\n","title":"Turing.Inference.build_values_vnt"},{"category":"method","location":"api/Inference/#Turing.Inference.check_all_variables_handled-Tuple{VarNamedTuple, Gibbs}","page":"Inference","text":"check_all_variables_handled(vnt, spl::Gibbs)\n\nCheck that every variable in vnt belongs to a component.\n\nA key of vnt no declared varname subsumes is examined leaf by leaf, because ownership of a value stored as one key is a property of its leaves. One component owning every leaf can be handed the whole value, so that partition is fine; several owning parts of it is one Gibbs cannot express, since freeing one part of a single stored value is exactly what conditioned_values cannot do; and a leaf no component owns is one the user left out.\n\n\n\n\n\n","title":"Turing.Inference.check_all_variables_handled"},{"category":"method","location":"api/Inference/#Turing.Inference.check_block_nonempty-Tuple{Any, Any}","page":"Inference","text":"check_block_nonempty(varnames, block)\n\nThrow if the model currently reaches none of the variables a component samples.\n\nA component with nothing to sample is refused rather than skipped. MH would in fact sample such a partition correctly, and skipping the component for that sweep is the sensible semantics, but a LogDensityFunction over no variables is ill-formed and fails deep inside with VectorEvaluator requires a vector of floating-point values. Refusing uniformly is loud and easy to revisit; letting it through for some samplers and not others is neither.\n\n\n\n\n\n","title":"Turing.Inference.check_block_nonempty"},{"category":"method","location":"api/Inference/#Turing.Inference.check_no_missing_arguments-Tuple{DynamicPPL.Model}","page":"Inference","text":"check_no_missing_arguments(model)\n\nRefuse a model with an argument bound to missing.\n\nGibbs conditions every component on the variables it does not sample, and condition cannot take precedence over a missing argument: the missing reaches the likelihood and throws MethodError: no method matching loglikelihood(::Normal{Float64}, ::Missing).\n\nGibbs's restriction, not the model's, so it belongs here and not in _check_model: a model taking x as an argument and drawing x ~ Normal(m, 1) from it samples to the right posterior under MH when called with missing.\n\nA whole argument only. An array holding a missing is a different mechanism – the element is latent while its neighbours are data, and conditioning reaches it element by element – so an argument of [1.5, missing] samples under Gibbs and must not be caught here.\n\n\n\n\n\n","title":"Turing.Inference.check_no_missing_arguments"},{"category":"method","location":"api/Inference/#Turing.Inference.check_reported_variables-Tuple{Any, VarNamedTuple, VarNamedTuple}","page":"Inference","text":"check_reported_variables(varnames, conditioned, report)\n\nThrow if report contains a value for a variable the component did not sample and was conditioned on.\n\ngibbs_get_parameter_values' contract is that a component reports only what it samples, and merge gives the report priority, so a foreign value silently replaces another block's – either frozen for the rest of the run, or re-sampled afterwards from a corrupted conditional, which looks perfectly plausible in the chain.\n\nA component may legitimately report a variable it does not sample when that variable did not exist to be conditioned on and its own step brought it into being: it is assumed rather than observed, so it lands in the component's accumulator. That case is not an error here – check_variable_set's appearance branch diagnoses it, and names the component that should have sampled it. Only a variable that was conditioned can have been overwritten.\n\n\n\n\n\n","title":"Turing.Inference.check_reported_variables"},{"category":"method","location":"api/Inference/#Turing.Inference.check_variable_set-Tuple{Gibbs, Any, Any, Turing.Inference.Snapshot, Turing.Inference.Snapshot, VarNamedTuple}","page":"Inference","text":"check_variable_set(spl, sampler, varnames, old, new, proposed)\n\nCheck that a change in the set of variables is one the component that made it may make.\n\nThis is a best-effort check, not an enforcement of the rule in the Gibbs docstring. It compares the snapshots either side of a component's step, so it sees only what that component accepted: a proposal that crossed and was rejected leaves both snapshots equal and passes. In practice it is reliable, because the deciding variable usually does move – on the bad_dimension model in the Gibbs docstring it refused the split partition on all 40 seeds tried, at 50, 300 and 2000 draws alike – but that is a fact about those chains, not a guarantee. Returning normally therefore says nothing about whether the partition is valid; meeting the requirement is the caller's job.\n\nOnly a variable appearing or leaving is checked. A component moving the support of another block's variable is permitted, and deliberately unchecked; see the warning in the Gibbs docstring for what that costs and who owns it.\n\nTwo conditions have to hold together, and anything else throws:\n\nthe component taking the step declares allow_varying_dimension; and\nthe variable that came or went is one that same component samples.\n\nThe first is needed because during a component's step every variable it does not sample is conditioned, and so cannot move: if the set of variables the model reaches changed, it was that component's own proposal that moved between two different supports. The second is needed because a variable coming and going under one component's proposal while another component samples it means that other component conditions on a variable absent from the state being proposed – the two blocks disagree about the support, and the chain comes back biased even though both samplers can handle varying dimension on their own.\n\nTogether they say: the variable and whatever decides whether it exists belong in one block, sampled by a component built for it. The cost of getting this wrong is not a crash but a wrong answer: on the dynamic_bernoulli_normal model in the tests, where b decides whether θ[2] exists, Gibbs(@varname(b) => MH(), @varname(θ) => PG(20)) used to sample and return P(b=1) between 0.0 and 0.18 against an exact 0.394, while Gibbs((@varname(b), @varname(θ)) => PG(20)) gives 0.38.\n\nBoth directions are checked, and symmetrically: old and new are both the set of variables a model evaluation reached (see reached_values), so a leaf in one and not the other came or went during this step, and nothing else can have caused it – every variable outside the component's block was conditioned.\n\nTaking both sets from the model, rather than from what the component reported, is what makes the verdict independent of the draw Gibbs initialises with and of the component's own bookkeeping. A component is free to keep a value for a variable it is not currently sampling; were that value taken into the snapshot it would be conditioned into the step of whichever component decides the variable's existence, that step would never assume it again, and the appearance branch would be dead for it – so a split partition would be rejected only for those seeds whose initialising draw happened to miss the variable.\n\n\n\n\n\n","title":"Turing.Inference.check_variable_set"},{"category":"method","location":"api/Inference/#Turing.Inference.check_walkers_same_layout-Tuple{Any}","page":"Inference","text":"check_walkers_same_layout(linked_vis)\n\nThrow unless every walker has the same parameter layout.\n\nThe stretch move interpolates between two walkers' position vectors, and the single LogDensityFunction is built from the first walker, so every walker's vector is decoded against that one layout. All of them therefore have to describe the same variables in the same order. On a model whose set of variables depends on its own draws, walkers initialised from the prior land in different branches and do not.\n\nThe variables are compared, not the total length. Two branches can hold different variables and the same number of parameters – if b; x ~ Normal(); else; y ~ Normal(); end – and comparing lengths passed those walkers, after which sampling threw KeyError: key y not found from inside the decode.\n\n\n\n\n\n","title":"Turing.Inference.check_walkers_same_layout"},{"category":"method","location":"api/Inference/#Turing.Inference.component_stats-Tuple{Gibbs, Any}","page":"Inference","text":"component_stats(spl::Gibbs, states)\n\nCollect the component samplers' statistics into one NamedTuple, prefixing each with the symbols of the variables that component samples, so that two components reporting e.g. acceptance_rate do not collide. Components sampling the same symbols are further distinguished by their index.\n\nThe prefix uses each variable's symbol rather than its whole VarName because chain packages read variable structure back out of these names: MCMCChains.namesingroup(chn, :x) matches anything beginning x[, so a statistic named x[1]_acceptance_rate would be served up as one of x's draws. A symbol carries no optic, so it cannot be parsed as part of another variable.\n\n\n\n\n\n","title":"Turing.Inference.component_stats"},{"category":"method","location":"api/Inference/#Turing.Inference.conditioned_values-Tuple{VarNamedTuple, AbstractVector{<:AbstractPPL.VarName}}","page":"Inference","text":"conditioned_values(global_vnt, target_variables)\n\nReturn the values in global_vnt for every variable not sampled by this Gibbs component, i.e. the ones it conditions on.\n\nA component may own part of a value the model stores as a unit, which cannot be expressed: under x ~ MvNormal(zeros(2), I) the values hold x as one key, so freeing it for a component that samples x[1] would free x[2] too. This throws rather than hand a component a larger block than it owns. Whether a partition is expressible is a property of the model's tilde statements and not of the samplers – element-wise x[1] ~ and x[2] ~ give a key each and split cleanly, because the snapshot comes from a model evaluation rather than from what a component reports. Sampling a genuinely unsplittable block means writing a sampler against the AbstractMCMC interface directly.\n\nConditioned variables reach tilde_observe!!, so particle samplers reweight on them. That is what makes the component's target distribution correct: a conditioned variable the target depends on must reweight the sweep, and one it does not contributes the same increment to every particle, which ESS-gated resampling ignores.\n\n\n\n\n\n","title":"Turing.Inference.conditioned_values"},{"category":"method","location":"api/Inference/#Turing.Inference.externalsampler-Tuple{AbstractMCMC.AbstractSampler}","page":"Inference","text":"externalsampler(\n    sampler::AbstractSampler;\n    adtype=AutoForwardDiff(),\n    unconstrained=AbstractMCMC.requires_unconstrained_space(sampler),\n)\n\nWrap a sampler so it can be used as an inference algorithm.\n\nArguments\n\nsampler::AbstractSampler: The sampler to wrap.\n\nKeyword Arguments\n\nadtype::ADTypes.AbstractADType=ADTypes.AutoForwardDiff(): The automatic differentiation (AD) backend to use.\nunconstrained::Bool=AbstractMCMC.requires_unconstrained_space(sampler): Whether the sampler requires unconstrained space.\n\n\n\n\n\n","title":"Turing.Inference.externalsampler"},{"category":"method","location":"api/Inference/#Turing.Inference.find_initial_params_ldf-Tuple{Random.AbstractRNG, LogDensityFunction, DynamicPPL.AbstractInitStrategy}","page":"Inference","text":"find_initial_params_ldf(rng, ldf, init_strategy; max_attempts=1000)\n\nGiven a LogDensityFunction and an initialization strategy, attempt to find valid initial parameters by sampling from the initialization strategy and checking that the log density (and gradient, if available) are finite. If valid parameters are not found after max_attempts, throw an error.\n\n\n\n\n\n","title":"Turing.Inference.find_initial_params_ldf"},{"category":"method","location":"api/Inference/#Turing.Inference.fork-Tuple{Turing.Inference.Particle, Random.AbstractRNG}","page":"Inference","text":"fork(particle, rng)\n\nCopy particle into an independent, reseeded continuation. deepcopy forks the underlying TapedTask (Libtask defines copy as deepcopy) and preserves the task↔particle back-reference; reseed! then gives it its own random stream.\n\n\n\n\n\n","title":"Turing.Inference.fork"},{"category":"method","location":"api/Inference/#Turing.Inference.gibbs_get_parameter_values-Tuple{Any}","page":"Inference","text":"Turing.Inference.gibbs_get_parameter_values(state)\n\nReturn a VarNamedTuple containing the parameter values of all variables in the sampler state.\n\nTuring's Gibbs sampler maintains, at all points during the sampling process, a single global VarNamedTuple that contains the raw values for all variables in the model. During the sampling process, it calls each component sampler in turn and rebuilds that VarNamedTuple around the new raw values returned by each sampler.\n\nThis function is used to pass that information from a component sampler to the Gibbs sampler. Note that this means that the VarNamedTuple returned by this function should only contain raw values for the variables that the component sampler is responsible for sampling, and should not contain any values for other variables. In particular it must leave out := quantities: they are not variables, so Gibbs would take one appearing inside a branch for a variable that appeared mid-run. DynamicPPL.get_parameter_values returns exactly the ~ values of a state whose accumulator holds both.\n\nA step need not reach every variable the component samples: a target inside a branch the model did not take has no value that sweep, and leaving it out is the right answer. A component that wants to remember such a value – to reuse it if the branch comes back – should keep it in its own state and not report it. Gibbs does not read the report to decide which variables exist: it evaluates the model (see reached_values), because existence is a property of the model at the current values and not of a component's bookkeeping. Reporting a value for a variable the model no longer reaches has no effect on the snapshot, and reporting one for a variable the component does not sample is an error.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_get_parameter_values"},{"category":"method","location":"api/Inference/#Turing.Inference.gibbs_get_raw_values-Tuple{Any}","page":"Inference","text":"Turing.Inference.gibbs_get_raw_values(state)\n\nDeprecated name for gibbs_get_parameter_values, still honoured so that a sampler written against it keeps working. Calls through to the new name, so a state that defines only the new one can still be asked by the old.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_get_raw_values"},{"category":"method","location":"api/Inference/#Turing.Inference.gibbs_get_stats-Tuple{Any}","page":"Inference","text":"Turing.Inference.gibbs_get_stats(state)\n\nReturn a NamedTuple of sampler statistics (acceptance rates, step sizes, and so on) for the last step taken from state.\n\nGibbs discards its component samplers' transitions – reading parameters off them would cost a model re-evaluation – so a component that wants its statistics to reach the chain has to carry them on its state. Defaults to no statistics.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_get_stats"},{"category":"method","location":"api/Inference/#Turing.Inference.gibbs_initial_values-Tuple{Any, Any, Gibbs, Any}","page":"Inference","text":"gibbs_initial_values(rng, model, spl, initial_params)\n\nReturn the values the sweep starts from, and check that every one of them has a component.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_initial_values"},{"category":"function","location":"api/Inference/#Turing.Inference.gibbs_initialstep_recursive","page":"Inference","text":"Take the first step of MCMC for the first component sampler, and call the same function recursively on the remaining samplers, until no samplers remain. Return the global VNT and a tuple of initial states for all component samplers.\n\nThe step_function argument should always be either AbstractMCMC.step or AbstractMCMC.step_warmup.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_initialstep_recursive"},{"category":"function","location":"api/Inference/#Turing.Inference.gibbs_recompute_ldf_and_params","page":"Inference","text":"gibbs_recompute_ldf_and_params(\n    old_ldf::LogDensityFunction,\n    model::Model,\n    global_vals::VarNamedTuple,\n    extra_accs=()\n)\n\nShared helper that is used in gibbs_update_state!! for any sampler that uses a LogDensityFunction.\n\nCreates a new LogDensityFunction from the newly conditioned model, then reevaluates the model to obtain the correct vectorised parameters corresponding to the raw values in global_vals.\n\nIf extra information is needed (e.g. log-probabilities), extra_accs can be used to pass in other accumulators to be used in the same model evaluation, to avoid having to recompute them later.\n\nReturns (new_ldf, new_params, accs) where accs is the set of accumulators after evaluation, from which extra accumulators (e.g. LogLikelihoodAccumulator) can be read.\n\nThe flat layout is built from global_vals rather than reused from old_ldf. A layout is keyed by tilde statement and carries each variable's transform, so a reused one is stale the moment the model takes a different branch – not only when a variable joins or leaves the block, but also when the same variables arrive under different keys, or with a different transform and so a different linked dimension. Evaluating against a stale layout fails inside DynamicPPL naming no variable. Built from the values, the layout is identical whenever the execution path is, which is exactly when the component's own state depends on the ordering, and differs only when that state was going to be wrong anyway.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_recompute_ldf_and_params"},{"category":"function","location":"api/Inference/#Turing.Inference.gibbs_step_recursive","page":"Inference","text":"Run a Gibbs step for the first varname/sampler/state tuple, and recursively call the same function on the tail, until there are no more samplers left.\n\nThe step_function argument should always be either AbstractMCMC.step or AbstractMCMC.step_warmup.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_step_recursive"},{"category":"function","location":"api/Inference/#Turing.Inference.gibbs_update_state!!","page":"Inference","text":"Turing.Inference.gibbs_update_state!!(\n    sampler::AbstractSampler, state, model::Model, global_vals::VarNamedTuple\n)\n\nUpdate the state of a Gibbs component sampler to be consistent with the new values in global_vals. Each sampler should implement a method for its respective state type.\n\nNote that the model argument passed in here will be 'conditioned' on the new values inside global_vals. Thus, evaluating it will reflect the log-probability associated with the new values.\n\nExactly what this function should do will depend on what the sampler state contains, but for example, it will often mean:\n\nUpdating any raw or vectorised values stored in the sampler state to be consistent with global_vals.\nReevaluating the (new) model to update any cached log-probabilities.\nUpdating any log-density callables (such as a DynamicPPL.LogDensityFunction) stored in the sampler state, to be consistent with the new model.\n\nFor examples of this, please see the implementations of this function for the samplers in Turing.jl. In particular, the HMC and ExternalSampler implementations work with LogDensityFunction and demonstrate how information such as that can be updated based on the new model.\n\nSee also the five-argument form, gibbs_update_state!!(sampler, state, model, global_vals, reshaped::ReshapedBlock), which Gibbs calls in place of this one when the block has a different set of variables from the one the sampler last stepped.\n\n\n\n\n\n","title":"Turing.Inference.gibbs_update_state!!"},{"category":"method","location":"api/Inference/#Turing.Inference.init_strategy-Tuple{AbstractMCMC.AbstractSampler}","page":"Inference","text":"Turing.Inference.init_strategy(spl::AbstractSampler)\n\nGet the default initialization strategy for a given sampler spl, i.e. how initial parameters for sampling are chosen if not specified by the user. By default, this is InitFromPrior(), which samples initial parameters from the prior distribution.\n\n\n\n\n\n","title":"Turing.Inference.init_strategy"},{"category":"method","location":"api/Inference/#Turing.Inference.isgibbscomponent-Tuple{AbstractMCMC.AbstractSampler}","page":"Inference","text":"isgibbscomponent(spl::AbstractSampler)\n\nDeprecated name for supports_gibbs, still honoured so that a sampler written against it keeps working.\n\nDefined in terms of supports_gibbs rather than as a constant true, so that a wrapper delegating through this name – isgibbscomponent(w::MyWrapper) = isgibbscomponent(w.inner) – still gets the right answer for a sampler Turing declares unusable.\n\n\n\n\n\n","title":"Turing.Inference.isgibbscomponent"},{"category":"method","location":"api/Inference/#Turing.Inference.isreference-Tuple{Turing.Inference.Particle}","page":"Inference","text":"Whether particle is a conditional-SMC reference, i.e. pinned to a retained trajectory. Carried on the particle rather than inferred from its slot, so forking and resampling cannot get it wrong.\n\n\n\n\n\n","title":"Turing.Inference.isreference"},{"category":"method","location":"api/Inference/#Turing.Inference.loadstate-Tuple{VNChain}","page":"Inference","text":"Turing.loadstate(chain::FlexiChain{<:VarName})\n\nExtracts the last sampler state from a FlexiChain. This is the same function as  FlexiChains.last_sampler_state.\n\nnote: Returns a vector\nThis function always returns a vector of sampler states, even if there is only one chain. Consequently, if you are resuming a single-chain MCMC run like sample(model, spl, N), you will need to extract the sole element of the returned vector before passing it as the initial_state keyword argument to sample(). Please see the 'using with Turing' page, or the Turing.jl documentation page on initial_state, for more explanation of this.\n\n\n\n\n\n","title":"Turing.Inference.loadstate"},{"category":"method","location":"api/Inference/#Turing.Inference.log_proposal_density-Tuple{DynamicPPL.OnlyAccsVarInfo, InitFromPrior, VarNamedTuple}","page":"Inference","text":"log_proposal_density(\n    old_vi::DynamicPPL.OnlyAccsVarInfo,\n    init_strategy_given_new::DynamicPPL.AbstractInitStrategy,\n    old_unspecified_priors::DynamicPPL.VarNamedTuple\n)\n\nCalculate the ratio g(x|x') where g is the proposal distribution used to generate x (represented by old_vi), given the new state x'.\n\nIf the arguments are switched (i.e., new_vi is passed as the first argument, and init_strategy_given_old as the second), the function calculates g(x'|x).\n\nThe log-density of the proposal distribution is calculated by summing up the contributions from:\n\nany variables that have an explicit proposal in init_strategy_given_new (i.e., those in spl.vns_with_proposal), which can be either static or conditional proposals; and\nany variables that do not have an explicit proposal, for which we defer to its prior distribution.\n\n\n\n\n\n","title":"Turing.Inference.log_proposal_density"},{"category":"method","location":"api/Inference/#Turing.Inference.particle_rng-Tuple{Random.AbstractRNG}","page":"Inference","text":"A fresh counter-based generator for one particle, seeded from rng.\n\n\n\n\n\n","title":"Turing.Inference.particle_rng"},{"category":"method","location":"api/Inference/#Turing.Inference.post_sample_hook-Tuple{Any, AbstractMCMC.AbstractSampler}","page":"Inference","text":"post_sample_hook(chain, sampler::AbstractSampler; kwargs...)\n\nA post-sampling hook that can e.g. print info about the results of sampling.\n\nImplementations of this should be careful to take kwargs... as keyword arguments instead of restricting the signature to specific keyword arguments. Right now, the only keyword argument that is passed to this function is verbose, but in the future additional keyword arguments may be passed here.\n\n\n\n\n\n","title":"Turing.Inference.post_sample_hook"},{"category":"method","location":"api/Inference/#Turing.Inference.post_sample_hook-Tuple{VNChain, Union{HMC, HMCDA, NUTS}}","page":"Inference","text":"post_sample_hook(chain::FlexiChains.VNChain, sampler::Union{HMC,NUTS,HMCDA}; kwargs...)\n\nEmit a warning message if there are divergent transitions in the chain.\n\n\n\n\n\n","title":"Turing.Inference.post_sample_hook"},{"category":"method","location":"api/Inference/#Turing.Inference.reached_values-Tuple{Any, Any, VarNamedTuple}","page":"Inference","text":"reached_values(rng, model, proposed)\n\nReturn the values of the variables model reaches when evaluated at proposed.\n\nThis is how the snapshot after a component's step is built, rather than from the values the component reports. Which variables exist is a property of the model at the current values, so only an evaluation settles it, and a component's report cannot be trusted for it: a component is free to keep a value for a variable it is not currently sampling, which is useful to it and none of Gibbs's business, but were that value to enter the snapshot it would be conditioned into the step of whichever component decides the variable's existence, and check_variable_set would never see the variable come back.\n\nThe evaluation costs one model evaluation per component step, and draws from the prior only for a variable proposed has no value for. That happens when a component under-reports a variable it samples, or when a variable appeared during this step, and check_variable_set throws in both cases – so nothing that returns normally has consumed rng.\n\n\n\n\n\n","title":"Turing.Inference.reached_values"},{"category":"function","location":"api/Inference/#Turing.Inference.resample_indices","page":"Inference","text":"Draw n ancestor indices from 1:length(weights) with probabilities weights.\n\n\n\n\n\n","title":"Turing.Inference.resample_indices"},{"category":"method","location":"api/Inference/#Turing.Inference.reseed!-Tuple{Turing.Inference.Particle, Random.AbstractRNG}","page":"Inference","text":"reseed!(particle, rng)\n\nRestart particle as a fresh continuation seeded from rng, so that a particle descended from the reference stops reusing retained values and samples afresh. Mutates and returns particle.\n\n\n\n\n\n","title":"Turing.Inference.reseed!"},{"category":"method","location":"api/Inference/#Turing.Inference.should_resample-Tuple{Turing.Inference.AbstractResampler, Any}","page":"Inference","text":"Whether to resample given the normalized weights. Bare schemes always resample.\n\n\n\n\n\n","title":"Turing.Inference.should_resample"},{"category":"method","location":"api/Inference/#Turing.Inference.supports_gibbs-Tuple{AbstractMCMC.AbstractSampler}","page":"Inference","text":"supports_gibbs(spl::AbstractSampler)\n\nReturn a boolean indicating whether spl is a valid component for a Gibbs sampler.\n\nDefaults to true if no method has been defined for a particular sampler.\n\n\n\n\n\n","title":"Turing.Inference.supports_gibbs"},{"category":"method","location":"api/Inference/#Turing.Inference.used_proposals-Tuple{Any}","page":"Inference","text":"used_proposals(vi)\n\nThe variables of vi whose declared proposal init actually drew from.\n\n\n\n\n\n","title":"Turing.Inference.used_proposals"},{"category":"method","location":"api/Inference/#Turing.Inference.weight_ess-Tuple{Any}","page":"Inference","text":"Effective sample size of a normalised weight vector, 1 / Σ wᵢ². Named for the weights to keep it distinct from MCMCDiagnosticTools.ess, which Turing re-exports and which measures a chain's autocorrelation rather than a population's weight degeneracy.\n\n\n\n\n\n","title":"Turing.Inference.weight_ess"},{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Module-wide-re-exports","page":"API","text":"Turing.jl directly re-exports the entire public API of Distributions.jl. Please see its documentation for more details.","title":"Module-wide re-exports"},{"category":"section","location":"api/#Individual-exports-and-re-exports","page":"API","text":"In this API documentation, for the sake of clarity, we have listed the module that actually defines each of the exported symbols. Note, however, that all of the following symbols are exported unqualified by Turing. That means, for example, you can just write\n\nusing Turing\n\n@model function my_model() end\n\nsample(my_model(), Prior(), 100)\n\ninstead of\n\nDynamicPPL.@model function my_model() end\n\nsample(my_model(), Turing.Inference.Prior(), 100)\n\neven though Prior() is actually defined in the Turing.Inference module and @model in the DynamicPPL package.","title":"Individual exports and re-exports"},{"category":"section","location":"api/#Modelling","page":"API","text":"Exported symbol Documentation Description\n@model DynamicPPL.@model Define a probabilistic model\n@varname AbstractPPL.@varname Generate a VarName from a Julia expression\nto_submodel DynamicPPL.to_submodel Define a submodel\nprefix DynamicPPL.prefix Prefix all variable names in a model with a given VarName\nLogDensityFunction DynamicPPL.LogDensityFunction A struct containing all information about how to evaluate a model. Mostly for advanced users\n@addlogprob! DynamicPPL.@addlogprob! Add arbitrary log-probability terms during model evaluation\nsetthreadsafe DynamicPPL.setthreadsafe Mark a model as requiring threadsafe evaluation\nmight_produce Libtask.might_produce Mark a method signature as potentially calling Libtask.produce\n@might_produce Libtask.@might_produce Mark a function name as potentially calling Libtask.produce\nset_logprob_type! DynamicPPL.set_logprob_type! Set the base log-probability type used during evaluation of Turing models","title":"Modelling"},{"category":"section","location":"api/#Inference","page":"API","text":"Exported symbol Documentation Description\nsample StatsBase.sample Sample from a model\nMCMCThreads AbstractMCMC.MCMCThreads Run MCMC using multiple threads\nMCMCDistributed AbstractMCMC.MCMCDistributed Run MCMC using multiple processes\nMCMCSerial AbstractMCMC.MCMCSerial Run MCMC using without parallelism\nloadstate Turing.Inference.loadstate Load saved state from an MCMC chain\nVNChain n/a Alias for FlexiChain{VarName}","title":"Inference"},{"category":"section","location":"api/#Samplers","page":"API","text":"Exported symbol Documentation Description\nPrior Turing.Inference.Prior Sample from the prior distribution\nMH Turing.Inference.MH Metropolis–Hastings\nEmcee Turing.Inference.Emcee Affine-invariant ensemble sampler\nESS Turing.Inference.ESS Elliptical slice sampling\nGibbs Turing.Inference.Gibbs Gibbs sampling\nGibbsConditional Turing.Inference.GibbsConditional Gibbs sampling with analytical conditional posterior distributions\nHMC Turing.Inference.HMC Hamiltonian Monte Carlo\nSGLD Turing.Inference.SGLD Stochastic gradient Langevin dynamics\nSGHMC Turing.Inference.SGHMC Stochastic gradient Hamiltonian Monte Carlo\nPolynomialStepsize Turing.Inference.PolynomialStepsize Returns a function which generates polynomially decaying step sizes\nHMCDA Turing.Inference.HMCDA Hamiltonian Monte Carlo with dual averaging\nNUTS Turing.Inference.NUTS No-U-Turn Sampler\nSMC Turing.Inference.SMC Sequential Monte Carlo\nPG Turing.Inference.PG Particle Gibbs\nCSMC Turing.Inference.CSMC The same as PG\nRepeatSampler Turing.Inference.RepeatSampler A sampler that runs multiple times on the same variable\nexternalsampler Turing.Inference.externalsampler Wrap an external sampler for use in Turing","title":"Samplers"},{"category":"section","location":"api/#Data-structures","page":"API","text":"Exported symbol Documentation Description\n@vnt DynamicPPL.VarNamedTuples.@vnt Generate a VarNameTuple\nVarNamedTuple DynamicPPL.VarNamedTuples.VarNamedTuple A mapping from VarNames to values\nOrderedDict OrderedCollections.OrderedDict An ordered dictionary","title":"Data structures"},{"category":"section","location":"api/#DynamicPPL-utilities","page":"API","text":"Please see the generated quantities and probability interface guides for more information.\n\nExported symbol Documentation Description\nreturned DynamicPPL.returned Calculate additional quantities defined in a model\npredict StatsAPI.predict Generate samples from posterior predictive distribution\npointwise_logdensities DynamicPPL.pointwise_logdensities Compute log densities (both prior and likelihood) for each sample in a chain\npointwise_loglikelihoods DynamicPPL.pointwise_loglikelihoods Compute log likelihoods for each sample in a chain\npointwise_prior_logdensities DynamicPPL.pointwise_prior_logdensities Compute log priors for each sample in a chain\nlogprior DynamicPPL.logprior Compute log prior probability\nlogjoint DynamicPPL.logjoint Compute log joint probability\ncondition AbstractPPL.condition Condition a model on data\ndecondition AbstractPPL.decondition Remove conditioning on data\nconditioned DynamicPPL.conditioned Return the conditioned values of a model\nfix DynamicPPL.fix Fix the value of a variable\nunfix DynamicPPL.unfix Unfix the value of a variable","title":"DynamicPPL utilities"},{"category":"section","location":"api/#Initialisation-strategies","page":"API","text":"Turing.jl provides several strategies to initialise parameters for models.\n\nExported symbol Documentation Description\nInitFromPrior DynamicPPL.InitFromPrior Obtain initial parameters from the prior distribution\nInitFromUniform DynamicPPL.InitFromUniform Obtain initial parameters by sampling uniformly in linked space\nInitFromParams DynamicPPL.InitFromParams Manually specify (possibly a subset of) initial parameters","title":"Initialisation strategies"},{"category":"section","location":"api/#Variational-inference","page":"API","text":"See the docs of AdvancedVI.jl for detailed usage and the variational inference tutorial for a basic walkthrough.\n\nExported symbol Documentation Description\nvi Turing.vi Perform variational inference\nq_locationscale Turing.Variational.q_locationscale Find a numerically non-degenerate initialization for a location-scale variational family\nq_meanfield_gaussian Turing.Variational.q_meanfield_gaussian Find a numerically non-degenerate initialization for a mean-field Gaussian family\nq_fullrank_gaussian Turing.Variational.q_fullrank_gaussian Find a numerically non-degenerate initialization for a full-rank Gaussian family\nKLMinRepGradDescent AdvancedVI.KLMinRepGradDescent KL divergence minimization via stochastic gradient descent with the reparameterization gradient\nKLMinRepGradProxDescent AdvancedVI.KLMinRepGradProxDescent KL divergence minimization via stochastic proximal gradient descent with the reparameterization gradient over location-scale variational families\nKLMinScoreGradDescent AdvancedVI.KLMinScoreGradDescent KL divergence minimization via stochastic gradient descent with the score gradient\nKLMinWassFwdBwd AdvancedVI.KLMinWassFwdBwd KL divergence minimization via Wasserstein proximal gradient descent\nKLMinNaturalGradDescent AdvancedVI.KLMinNaturalGradDescent KL divergence minimization via natural gradient descent\nKLMinSqrtNaturalGradDescent AdvancedVI.KLMinSqrtNaturalGradDescent KL divergence minimization via natural gradient descent in the square-root parameterization\nFisherMinBatchMatch AdvancedVI.FisherMinBatchMatch Covariance-weighted Fisher divergence minimization via the batch-and-match algorithm","title":"Variational inference"},{"category":"section","location":"api/#Automatic-differentiation-types","page":"API","text":"These are used to specify the automatic differentiation backend to use. See the AD guide for more information.\n\nExported symbol Documentation Description\nAutoEnzyme ADTypes.AutoEnzyme Enzyme.jl backend\nAutoForwardDiff ADTypes.AutoForwardDiff ForwardDiff.jl backend\nAutoMooncake ADTypes.AutoMooncake Mooncake.jl backend\nAutoReverseDiff ADTypes.AutoReverseDiff ReverseDiff.jl backend","title":"Automatic differentiation types"},{"category":"section","location":"api/#Debugging","page":"API","text":"setprogress! is re-exported from AbstractMCMC. See AbstractMCMC.setprogress!.","title":"Debugging"},{"category":"section","location":"api/#Distributions","page":"API","text":"These distributions are defined in DynamicPPL and re-exported.\n\nExported symbol Documentation\nFlat DynamicPPL.Flat\nFlatPos DynamicPPL.FlatPos\nBinomialLogit DynamicPPL.BinomialLogit\nOrderedLogistic DynamicPPL.OrderedLogistic\nLogPoisson DynamicPPL.LogPoisson","title":"Distributions"},{"category":"section","location":"api/#Tools-to-work-with-distributions","page":"API","text":"Exported symbol Documentation Description\nI LinearAlgebra.I Identity matrix\nfilldist DynamicPPL.filldist Create a product distribution from a distribution and integers\narraydist DynamicPPL.arraydist Create a product distribution from an array of distributions\nNamedDist DynamicPPL.NamedDist A distribution that carries the name of the variable","title":"Tools to work with distributions"},{"category":"section","location":"api/#Point-estimates","page":"API","text":"See the mode estimation tutorial for more information.\n\nExported symbol Documentation Description\nmaximum_a_posteriori Turing.Optimisation.maximum_a_posteriori Find a MAP estimate for a model\nmaximum_likelihood Turing.Optimisation.maximum_likelihood Find a MLE estimate for a model\nMAP Turing.Optimisation.MAP Type to use with Optim.jl for MAP estimation\nMLE Turing.Optimisation.MLE Type to use with Optim.jl for MLE estimation\nvector_names_and_params Turing.Optimisation.vector_names_and_params Extract parameter names and values as vectors","title":"Point estimates"},{"category":"section","location":"api/RandomMeasures/#API:-random-measures","page":"Random measures","text":"Access the extension after loading Turing:\n\nusing Turing\n\nconst RandomMeasures = Base.get_extension(Turing, :TuringDistributionsExt)\nRandomMeasures.DirichletProcess(1.0)","title":"API: random measures"},{"category":"type","location":"api/RandomMeasures/#TuringDistributionsExt.ChineseRestaurantProcess","page":"Random measures","text":"ChineseRestaurantProcess(rpm, m)\n\nA Chinese restaurant process for the random probability measure rpm, where m contains the current cluster counts.\n\n\n\n\n\n","title":"TuringDistributionsExt.ChineseRestaurantProcess"},{"category":"type","location":"api/RandomMeasures/#TuringDistributionsExt.DirichletProcess","page":"Random measures","text":"DirichletProcess(α)\n\nA Dirichlet process with concentration parameter α.\n\nIts size-biased and stick-breaking representations draw proportions according to\n\nV_k sim operatornameBeta(1 alpha)\n\nIn the Chinese restaurant representation, an occupied cluster k has weight m[k], while a new cluster has weight α.\n\nReferences\n\nYee Whye Teh, \"Dirichlet Process,\" 2010. https://www.stats.ox.ac.uk/~teh/research/npbayes/Teh2010a.pdf\n\n\n\n\n\n","title":"TuringDistributionsExt.DirichletProcess"},{"category":"type","location":"api/RandomMeasures/#TuringDistributionsExt.PitmanYorProcess","page":"Random measures","text":"PitmanYorProcess(d, θ, t)\n\nA Pitman-Yor process with discount d, concentration θ, and t occupied clusters.\n\nIts size-biased and stick-breaking representations draw proportions according to\n\nV_k sim operatornameBeta(1 - d theta + t d)\n\nIn the Chinese restaurant representation, an occupied cluster k has weight m[k] - d, while a new cluster has weight θ + d * t.\n\nReferences\n\nJim Pitman and Marc Yor, \"The two-parameter Poisson-Dirichlet distribution derived from a stable subordinator,\" 1997.\n\n\n\n\n\n","title":"TuringDistributionsExt.PitmanYorProcess"},{"category":"type","location":"api/RandomMeasures/#TuringDistributionsExt.SizeBiasedSamplingProcess","page":"Random measures","text":"SizeBiasedSamplingProcess(rpm, surplus)\n\nA size-biased sampling representation of the random probability measure rpm with remaining mass surplus.\n\n\n\n\n\n","title":"TuringDistributionsExt.SizeBiasedSamplingProcess"},{"category":"type","location":"api/RandomMeasures/#TuringDistributionsExt.StickBreakingProcess","page":"Random measures","text":"StickBreakingProcess(rpm)\n\nA stick-breaking representation of the random probability measure rpm.\n\n\n\n\n\n","title":"TuringDistributionsExt.StickBreakingProcess"},{"category":"method","location":"api/RandomMeasures/#TuringDistributionsExt.stickbreak-Tuple{Any}","page":"Random measures","text":"stickbreak(v)\n\nConvert K - 1 breaking proportions in v into K simplex weights.\n\n\n\n\n\n","title":"TuringDistributionsExt.stickbreak"},{"category":"section","location":"#Turing.jl","page":"Home","text":"This site contains the API documentation for the identifiers exported by Turing.jl.\n\nIf you are looking for usage examples and guides, please visit https://turinglang.org/docs.","title":"Turing.jl"},{"category":"section","location":"api/Variational/#API:-Turing.Variational","page":"Variational ","text":"","title":"API: Turing.Variational"},{"category":"type","location":"api/Variational/#Turing.Variational.VIResult","page":"Variational ","text":"VIResult(ldf, q, info, state)\n\nldf: A DynamicPPL.LogDensityFunction corresponding to the target model (the original model can be accessed as ldf.model). If the VI process was run in unconstrained space, this LogDensityFunction will also be in unconstrained space.\nq: Output variational distribution of algorithm. Note that, as above, this will typically also be in unconstrained space.\nstate: Collection of states used by algorithm. This can be used to resume from a past call to vi.\ninfo: Information generated while executing algorithm.\n\n\n\n\n\n","title":"Turing.Variational.VIResult"},{"category":"method","location":"api/Variational/#Base.rand-Tuple{Random.AbstractRNG, Turing.Variational.VIResult, Vararg{Integer}}","page":"Variational ","text":"Base.rand(rng::Random.AbstractRNG, res::VIResult, sz...)\n\nDraw a sample, or array of samples, from the variational distribution q in res. Each sample is a DynamicPPL.VarNamedTuple containing raw parameter values.\n\n\n\n\n\n","title":"Base.rand"},{"category":"method","location":"api/Variational/#Turing.Variational.q_fullrank_gaussian-Tuple{Random.AbstractRNG, LogDensityFunction}","page":"Variational ","text":"q_fullrank_gaussian(\n    [rng::Random.AbstractRNG,]\n    ldf::DynamicPPL.LogDensityFunction;\n    location::Union{Nothing,<:AbstractVector} = nothing,\n    scale::Union{Nothing,<:LowerTriangular} = nothing,\n    kwargs...\n)\n\nFind a numerically non-degenerate Gaussian q with a scale with full-rank factors (traditionally referred to as a \"full-rank family\") for approximating the target ldf::LogDensityFunction.\n\nIf the scale set as nothing, the default value will be a zero-mean Gaussian with a LowerTriangular scale matrix (resulting in a covariance with \"full-rank\" factors) no larger than 0.6*I (covariance of 0.6^2*I). This guarantees that the samples from the initial variational approximation will fall in the range of (-2, 2) with 99.9% probability, which mimics the behavior of the Turing.InitFromUniform() strategy. Whether the default choice is used or not, the scale may be adjusted via q_initialize_scale so that the log-densities of model are finite over the samples from q.\n\nArguments\n\nldf: The target log-density function.\n\nKeyword Arguments\n\nlocation: The location parameter of the initialization. If nothing, a vector of zeros is used.\nscale: The scale parameter of the initialization. If nothing, an identity matrix is used.\n\nThe remaining keyword arguments are passed to q_locationscale.\n\nReturns\n\nAn AdvancedVI.LocationScale distribution matching the support of ldf.\n\n\n\n\n\n","title":"Turing.Variational.q_fullrank_gaussian"},{"category":"method","location":"api/Variational/#Turing.Variational.q_initialize_scale-Tuple{Random.AbstractRNG, LogDensityFunction, AbstractVector, AbstractMatrix, UnivariateDistribution}","page":"Variational ","text":"q_initialize_scale(\n    rng::Random.AbstractRNG,\n    ldf::DynamicPPL.LogDensityFunction,\n    location::AbstractVector,\n    scale::AbstractMatrix,\n    basedist::Distributions.UnivariateDistribution;\n    num_samples::Int = 10,\n    num_max_trials::Int = 10,\n    reduce_factor::Real = one(eltype(scale)) / 2\n)\n\nGiven an initial location-scale distribution q formed by location, scale, and basedist, shrink scale until the expectation of log-densities of ldf taken over q are finite. If the log-densities are not finite even after num_max_trials, throw an error.\n\nFor reference, a location-scale distribution q formed by location, scale, and basedist is a distribution where its sampling process z sim q can be represented as\n\nu = rand(basedist, d)\nz = scale * u + location\n\nArguments\n\nldf: The target log-density function.\nlocation: The location parameter of the initialization.\nscale: The scale parameter of the initialization.\nbasedist: The base distribution of the location-scale family.\n\nKeyword Arguments\n\nnum_samples: Number of samples used to compute the average log-density at each trial.\nnum_max_trials: Number of trials until throwing an error.\nreduce_factor: Factor for shrinking the scale. After n trials, the scale is then scale*reduce_factor^n.\n\nReturns\n\nscale_adj: The adjusted scale matrix matching the type of scale.\n\n\n\n\n\n","title":"Turing.Variational.q_initialize_scale"},{"category":"method","location":"api/Variational/#Turing.Variational.q_locationscale-Tuple{Random.AbstractRNG, LogDensityFunction}","page":"Variational ","text":"q_locationscale(\n    [rng::Random.AbstractRNG,]\n    ldf::DynamicPPL.LogDensityFunction;\n    location::Union{Nothing,<:AbstractVector} = nothing,\n    scale::Union{Nothing,<:Diagonal,<:LowerTriangular} = nothing,\n    meanfield::Bool = true,\n    basedist::Distributions.UnivariateDistribution = Normal()\n)\n\nFind a numerically non-degenerate variational distribution q for approximating the target LogDensityFunction within the location-scale variational family formed by the type of scale and basedist.\n\nThe distribution can be manually specified by setting location, scale, and basedist. Otherwise, it chooses a Gaussian with zero-mean and scale 0.6*I (covariance of 0.6^2*I) by default. This guarantees that the samples from the initial variational approximation will fall in the range of (-2, 2) with 99.9% probability, which mimics the behavior of the Turing.InitFromUniform() strategy.\n\nWhether the default choice is used or not, the scale may be adjusted via q_initialize_scale so that the log-densities of model are finite over the samples from q. If meanfield is set as true, the scale of q is restricted to be a diagonal matrix and only the diagonal of scale is used.\n\nFor reference, a location-scale distribution q formed by location, scale, and basedist is a distribution where its sampling process z sim q can be represented as\n\nu = rand(basedist, d)\nz = scale * u + location\n\nArguments\n\nldf: The target log-density function.\n\nKeyword Arguments\n\nlocation: The location parameter of the initialization. If nothing, a vector of zeros is used.\nscale: The scale parameter of the initialization. If nothing, an identity matrix is used.\nmeanfield: Whether to use the mean-field approximation. If true, scale is converted into a Diagonal matrix. Otherwise, it is converted into a LowerTriangular matrix.\nbasedist: The base distribution of the location-scale family.\n\nThe remaining keywords are passed to q_initialize_scale.\n\nReturns\n\nAn AdvancedVI.LocationScale distribution matching the support of ldf.\n\n\n\n\n\n","title":"Turing.Variational.q_locationscale"},{"category":"method","location":"api/Variational/#Turing.Variational.q_meanfield_gaussian-Tuple{Random.AbstractRNG, LogDensityFunction}","page":"Variational ","text":"q_meanfield_gaussian(\n    [rng::Random.AbstractRNG,]\n    ldf::DynamicPPL.LogDensityFunction;\n    location::Union{Nothing,<:AbstractVector} = nothing,\n    scale::Union{Nothing,<:Diagonal} = nothing,\n    kwargs...\n)\n\nFind a numerically non-degenerate mean-field Gaussian q for approximating the target ldf::LogDensityFunction.\n\nIf the scale set as nothing, the default value will be a zero-mean Gaussian with a Diagonal scale matrix (the \"mean-field\" approximation) no larger than 0.6*I (covariance of 0.6^2*I). This guarantees that the samples from the initial variational approximation will fall in the range of (-2, 2) with 99.9% probability, which mimics the behavior of the Turing.InitFromUniform() strategy. Whether the default choice is used or not, the scale may be adjusted via q_initialize_scale so that the log-densities of model are finite over the samples from q.\n\nArguments\n\nldf: The target log-density function.\n\nKeyword Arguments\n\nlocation: The location parameter of the initialization. If nothing, a vector of zeros is used.\nscale: The scale parameter of the initialization. If nothing, an identity matrix is used.\n\nThe remaining keyword arguments are passed to q_locationscale.\n\nReturns\n\nAn AdvancedVI.LocationScale distribution matching the support of ldf.\n\n\n\n\n\n","title":"Turing.Variational.q_meanfield_gaussian"},{"category":"method","location":"api/Variational/#Turing.Variational.vi-Tuple{Random.AbstractRNG, DynamicPPL.Model, Any, Int64, Vararg{Any}}","page":"Variational ","text":"vi(\n    [rng::Random.AbstractRNG,]\n    model::DynamicPPL.Model,\n    family,\n    max_iter::Int;\n    adtype::ADTypes.AbstractADType=DEFAULT_ADTYPE,\n    algorithm::AdvancedVI.AbstractVariationalAlgorithm = KLMinRepGradProxDescent(\n        adtype; n_samples=10\n    ),\n    unconstrained::Bool=requires_unconstrained_space(algorithm),\n    fix_transforms::Bool=false,\n    show_progress::Bool = AbstractMCMC.PROGRESS[],\n    kwargs...\n)\n\nApproximate the target model via the variational inference algorithm algorithm using a variational family specified by family. This is a thin wrapper around AdvancedVI.optimize.\n\nThe default algorithm, KLMinRepGradProxDescent (relevant docs), assumes family returns a AdvancedVI.MvLocationScale, which is true if family is q_fullrank_gaussian or q_meanfield_gaussian. For other variational families, refer to the documentation of AdvancedVI to determine the best algorithm and other options.\n\nArguments\n\nmodel: The target DynamicPPL.Model.\nfamily: A function which is used to generate an initial variational approximation. Existing choices in Turing are q_locationscale, q_meanfield_gaussian, and q_fullrank_gaussian.\nmax_iter: Maximum number of steps.\nAny additional arguments are passed on to AdvancedVI.optimize.\n\nKeyword Arguments\n\nadtype: Automatic differentiation backend to be applied to the log-density. The default value for algorithm also uses this backend for differentiating the variational objective.\nalgorithm: Variational inference algorithm. The default is KLMinRepGradProxDescent, please refer to AdvancedVI docs for all the options.\nshow_progress: Whether to show the progress bar.\nunconstrained: Whether to transform the posterior to be unconstrained for running the variational inference algorithm. The default value depends on the chosen algorithm (most algorithms require unconstrained space).\nfix_transforms: Whether to precompute the transforms needed to convert model parameters to (possibly unconstrained) vectors. This can lead to performance improvements, but if any transforms depend on model parameters, setting fix_transforms=true can silently yield incorrect results.\nAny additional keyword arguments are passed on both to the function initial_approx, and also to AdvancedVI.optimize.\n\nSee the docs of AdvancedVI.optimize for additional keyword arguments.\n\nReturns\n\nA VIResult object: please see its docstring for information.\n\n\n\n\n\n","title":"Turing.Variational.vi"}]
}
